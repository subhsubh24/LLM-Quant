# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Data & Computation
pandas>=2.1.0
numpy>=1.26.0
requests>=2.31.0

# Machine Learning
# NOTE (ROADMAP A1): the heavy deep-learning / boosting stack (torch, torchvision,
# xgboost, lightgbm, statsmodels, pandas-datareader) was dropped when the stock/crypto
# ML modules (app/models, app/signals, app/data, app/features, app/strategies) were
# retired — no remaining module imports them, so this trims ~2GB+ from the deploy image.
# scikit-learn + cvxpy stay: still used (function-locally) by the kept asset-agnostic
# portfolio optimizer + backtest attribution.
scikit-learn>=1.4.0

# Portfolio Optimization
cvxpy>=1.4.0
# pypfopt removed - using cvxpy directly for optimization

# Database
sqlmodel>=0.0.14
aiosqlite>=0.19.0
# Postgres driver (Neon / any Postgres). Only used when DATABASE_URL points to
# Postgres; SQLite local-dev does not require it.
psycopg2-binary>=2.9.9

# LLM Integration — Google Gemini
google-genai>=0.3.0
httpx>=0.26.0

# Margin economics meter — non-blocking cost-per-outcome telemetry.
# Imported lazily (see backend/app/llm/analyst.py) so its absence from the
# CI manifest (requirements-ci.txt) does not break import-smoke or tests.
margin-meter @ git+https://github.com/subhsubh24/Margin.ai.git#subdirectory=sdk/python

# Real-time Data
websockets>=12.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Utilities
python-multipart>=0.0.6
scipy>=1.10.0
pytz>=2024.1
