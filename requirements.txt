# Core Data Science & ML
# numpy/pandas/scikit-learn/scipy stay: still used (function-locally) by the kept
# asset-agnostic backtest + monitoring modules.
numpy>=2.0.0
pandas>=3.0.0
scikit-learn>=1.3.0
scipy>=1.10.0

# NOTE (ROADMAP A1): the heavy deep-learning / boosting stack (tensorflow, xgboost,
# lightgbm) was dropped when the stock/crypto ML modules were retired — NOTHING in the
# prediction-markets app imports them (verified repo-wide). This mirrors the same cleanup
# already applied to backend/requirements.txt. SCOPE: this ROOT file is the LOCAL-DEV /
# demo manifest (README setup, scripts/run_demo.sh) — the Render/Docker BACKEND deploy
# installs backend/requirements.txt (render.yaml rootDir=backend; backend/Dockerfile), which
# was already trimmed. So this cleanup leans out local installs + removes a misleading
# phantom-ML-capability signal from the root manifest; it does not change the deploy bundle.

# Web & API
fastapi>=0.128.0
uvicorn>=0.40.0
aiohttp>=3.13.0
httpx>=0.28.0
requests>=2.31.0

# Configuration & Data
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
sqlmodel>=0.0.32
python-dotenv>=1.0.0

# Database (optional, for persistence)
redis>=7.0.0

# Utilities
pytz>=2023.3
websockets>=16.0

# Development & Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Alternative Data (all optional - providers fall back gracefully)
pytrends>=4.9.0          # Google Trends data (free, no key)
pandas-datareader>=0.10  # FRED data without API key
feedparser>=6.0.0        # RSS news feed parsing
