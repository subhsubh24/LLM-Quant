# LOOP HEALTH — is the LOOP getting better, or just busier?

> The deep audit (FACTORY_STANDARD §10) grades the **product**; the QUALITY_SCORECARD
> grades the **product**. This file grades the **loop itself** — whether the autonomous
> factory is *converging* (shipping durable, correct changes that move the DoD) or merely
> *churning* (re-attempting dead-ends, racking up reverts, walling on the same failure).

## Contract (read before editing)

- **Update EVERY run, in the bookkeeping PR, with REAL counts** — derived from `git`/`gh`
  and from what actually happened *this run*. Never hand-wave or round up.
- **Honest only.** Same anti-gaming rule as the governing number and the GO signal: a
  fabricated "improving" signal is a lie. If the loop churned, say `churning`. If it
  stalled, say `stuck`. The point of the metric is to catch that, not to look good.
- **Dashboard-readable.** The fenced `LOOP_HEALTH:` block below is parsed the same way as
  `BUSINESS_CASE_SUMMARY` / `GROWTH_STATUS` / `OWNER_ACTIONS` (fenced ```yaml, top-level
  key). Keep it well-formed.
- **Observability, NOT a ship gate.** `preflight.sh` does not block on this — it is a
  mirror the loop holds up to itself. (Contrast: the GO signal and the loss caps ARE gates.)
- **Classify every abandoned change** (`abandoned_reasons`) so the loop does not re-attempt
  the same dead-end — the build-loop's "don't repeat the failed path."
- **A `churning` or `stuck` signal is the trigger to open ONE
  `loop: harness improvement proposal` issue** — the ONLY channel by which the loop's own
  rules improve (it cannot edit its routine / `.claude`). A recurring wall that never raises
  a proposal is itself a dead signal. See FACTORY_STANDARD §10b.

`reason` ∈ `gate_tsc` · `gate_test` · `gate_determinism` · `gate_build` ·
`gate_backtest_nonreproduce` (this stack: a backtest that won't reproduce bit-for-bit) ·
`review_value` · `review_correctness` · `circuit_breaker` · `conflict` · `dead_end` ·
`blocked_owner` (handed to a human-core owner action — e.g. an egress/network or live-key
wall the loop must not route around).

`signal` ∈ `bootstrapping` (no prior data point to trend against) · `improving` (shipping
up, abandon/revert down vs. last run) · `steady` (converging at a stable rate) · `churning`
(high abandon/revert vs. shipped) · `stuck` (recurring failures / no convergence — open a
harness proposal).

```yaml
LOOP_HEALTH:
  project: LLM-Quant
  as_of: 2026-07-09
  last_run: 2026-07-09b         # a UNITS-CONTRACT correctness run, egress OPEN. Full 8-Haiku scout sweep across ALL tracks doubling as the ~daily DEEP AUDIT. Binding constraint STANDS (business_case_strength B, no validated real-money OOS edge; a research problem the sibling routine owns — it merged its own Run 17 #274 mid-run). Shipped ONE genuine code PR (#275) + this bookkeeping; correctly ABANDONED one candidate (#276, review_value); tracked one residual (NOPositionScanner confidence). #275 (B/correctness): orchestrator.kelly_size reads ScanResult.confidence ONLY for the `if confidence<min_confidence: skip` gate while Kelly sizes on win_probability=entry_price+edge; two ACTIVE default-scan strategies — CrossMarketArbitrageStrategy (0.85/0.80/0.60) and LogicalImplicationDetector (chain_conf/0.75) — emitted a confidence DECOUPLED from entry+edge, so a sub-0.5-fair-value BUY bypassed the gate (the #263/#268 class). Fix: shared gate_confidence(entry,edge)=clip(entry+edge,0,1) at every emission (edge UNCHANGED); test_confidence_units.py (in the gate) trips on pre-fix constants + proves the flip (a 0.40-fair-value BUY now sizes $0). 2 worktree-isolated Sonnet reviewers: A first-pass APPROVE (verified non-tautological by reverting all 8 sites); B took 2 HONESTY cycles — it correctly caught that my "last two remaining" completeness claim overclaimed (MarketMaking, then NOPositionScanner) → resolved by dropping ALL completeness language + independently enumerating all 7 default-scan strategies (2 fixed, 1 already-correct NearCertainty, 3 inert outcome_idx=-1 multi-leg skipped at skip_multi_leg, 1 residual). preflight GREEN; merged; 0 reverts. #276 ABANDONED (review_value): a DQ-gate volume/liquidity isfinite check that DUPLICATES #238's parser-level guard (both live parsers already isfinite-guard; can't fire on real data = churn, the #193 redundancy class). RESIDUAL: NOPositionScanner still carries a decoupled confidence (min(adjusted_rate*2,0.95)) but win_prob=adjusted_rate<0.5 by its longshot thesis and the *2 is a deliberate min_confidence bypass — an honest confidence would DISABLE the strategy, a design decision (ROADMAP B), not folded in. No DoD/floor box.
  _prev_run: 2026-07-09          # a LIVE-SAFETY (real-money-cap correctness) run, egress OPEN. Full 8-Haiku scout sweep across ALL tracks (model/alpha correctness, backtest/leakage, risk/live-safety, data/venue ingest, learning/research-integrity, quality/tests-coverage, security/abuse, quality-reconcile+self-validation). Binding constraint STANDS (business_case_strength B, no validated real-money OOS edge; a research problem a sibling routine owns). Shipped the MAXIMAL file-disjoint value-bar-clearing set the sweep surfaced: exactly ONE genuine code PR + this bookkeeping (NOT artificial scarcity: every other finding was verified NOTHING-GENUINE, churn on gated-off/dead code, or a wrong fix; see abandoned_reasons). #272 (D3/D4 live-safety): the live Polymarket order methods (_place_via_clob_client/_place_via_rest) built the fill OrderResult WITHOUT setting fees, defaulting to 0.0, while paper _simulate_fill charges filled_size*price*DEFAULT_FEE_RATE; on a live SELL _update_position nets result.fees+reconstructed entry_fee into record_realized_pnl (the hard loss-cap/kill-switch counter), so with fees=0 on live ONLY the entry fee was netted and the EXIT fee was silently dropped, so the caps undercounted real cash loss on the LIVE path by exactly the exit fee (asymmetry vs paper). Fix: both live paths set fees=filled_size*filled_price*DEFAULT_FEE_RATE (only filled_size>0). CONSERVATIVE direction (caps trip earlier); no double-count (record_execution is the sole fee-subtracting risk_manager call; entry fee reconstructed independently). 2 Sonnet reviewers (worktree-isolated) APPROVE; Reviewer B nit (drop a redundant end-to-end test + correct an over-general docstring) folded in pre-merge. 4 regression tests, 3 fail pre-fix. 0 reverts. No DoD/floor box (gated-live-path safety correctness, not a validated edge).
  _prev_run: 2026-07-08c         # the CORRECTNESS+RESEARCH-INTEGRITY+SECURITY+COVERAGE model-factory run (egress OPEN). Full 8-Haiku scout sweep (binding-constraint alpha · model/alpha correctness · backtest-integrity · risk/live-safety · learning-infra · quality/tests · security · quality-reconcile+self-validation). Binding constraint STANDS (business_case_strength B — no validated real-money OOS edge). Shipped the MAXIMAL file-disjoint value-bar-clearing set: 4 code PRs across 4 lenses + this bookkeeping; 8 first-round Sonnet reviewers (2/PR, all worktree-isolated), ALL first-pass APPROVE, 1 CI fix-cycle on #270 (gate_test), 1 proactive doc-nit fold-in on #268, 0 reverts. #267 (E/research-integrity): MarkToMarketEngine.check_resolutions() returned None implicitly on EVERY path → the forward-paper `resolutions booked` telemetry was permanently null (dead code, Research Run 16 #262 flagged URGENT); now returns the settled COUNT (0 on early guards + healthy no-resolution; N when N settle), incremented only after the persist-then-count ordering — settlement semantics byte-unchanged. #268 (B/correctness): NOPositionScanner (a LIVE default-scan strategy) emitted edge=Kelly-EV-per-dollar but the orchestrator reads win_probability=entry_price+edge (ABSOLUTE prob) → win_prob ABOVE 1.0 (2.03) fooling the COST-NET profitability gate into passing trades with true net edge ≤0; fix emits edge=P(reversal)-NO_price (the #263 class; OVERTURNS the repeatedly-dropped "clamp-harmless" prior WITH new evidence — the clamp bounds SIZE, never the GATE; incidentally repairs the calibration endpoint that clamped these trades to predicted_prob=1.0). #269 (G/security): pydantic ValidationError repr leaked SECRET VALUES (BACKEND_API_TOKEN in FULL, GEMINI prefix) into boot logs on a live-misconfigured boot; get_settings() now re-raises from e.errors(include_input=False) with `from None` — fails loud, names the missing cred, prints no secret. #270 (F/§26): registered 4 unregistered live-path regression tests (#264/#263/#253/#268 guards) into the blocking gate; DROPPED test_security_headers from the LIGHT gate (imports fastapi, which the light CI env omits — the CI RED caught what my fastapi-having local run masked; it stays out like test_backend_auth_fastapi). DEFERRED (recorded, NOT padded): LogicalImplication undersizing (conservative direction), empty-strategy record_pnl skip (arguably-correct + collided with #267), Manifold per-category method-probe (network-flaky, the research routine's job). No DoD/floor box. Binding constraint STANDS.
  _prev_run: 2026-07-08b         # the MODEL/STRATEGY-FACTORY run (egress OPEN). Full 8-Haiku scout sweep (binding-constraint alpha · model/alpha correctness · backtest-integrity · risk/live-safety · learning-infra · quality/tests · security · quality-reconcile+self-validation). Binding constraint STANDS (business_case_strength B — no validated real-money OOS edge). Shipped the MAXIMAL file-disjoint value-bar-clearing set: 3 code PRs across 3 lenses (correctness/safety/security) + this bookkeeping; 6 first-round Sonnet reviewers (ALL worktree-isolated), 2 fix cycles on #264 (both REQUEST_CHANGES resolved with the reviewer’s exact prescription + a fail-pre-fix test), 0 reverts. #263 (B/correctness): NearCertaintyStrategy — the ACTIVE default scanner — emitted edge=EV/price (RELATIVE) but the orchestrator reads win_probability=entry_price+edge (ABSOLUTE) for EVERY strategy → win_prob inflated above the strategy’s own `confidence` and ABOVE 1.0 at the low band (0.80→1.025), a systematic Kelly OVER-SIZING bug; fix emits the absolute EV. #264 (D3/safety): MAX_PER_TRADE_USD was read NOWHERE (owner cap = decoy; only max_position_usd=$50 gated a single order) → wired into the executor gate + get_executor, and RESIZE (not drop) the Kelly bet down to the cap in the sizer (gate-drop starves the paper-validation loop) + a 1e-9 money-precision tolerance so a NON-round cap’s float noise can’t reject the resized order. #265 (G/security): baseline security headers (nosniff/frame-DENY/referrer/HSTS, no CSP → /docs unbroken) + /health no longer leaks live_trading_enabled. DEFERRED (recorded, NOT padded): Sports-bucket alpha (concurrent #262 already probed → insufficient data), impact-coeff calibration (DORMANT param, multi-week), E4 auto-retirement (no real alpha/PnL stream → test-only behavior). No DoD/floor box. Binding constraint STANDS.
  _prev_run: 2026-07-08a        # an INTEGRITY-HARDENING + HONESTY-RECONCILE run (egress OPEN). Full 8-Haiku scout sweep (B8-nextstep · new-alpha · quality-reconcile · 4 deep-audit lenses: correctness/live-safety/leakage/artifact-honesty · F-coverage). No new alpha attempted (the binding constraint is a research problem the last 4 corpora refuted for the bucket family); this run's genuine value was integrity + honesty. Shipped 3 file-disjoint code PRs, maker≠checker, 6/6 reviewers first-pass APPROVE (0 fix cycles, 0 reverts). #258 F/§26 (registered the 3 UNGATED significance-net test suites — F11 bootstrap_oos_significance, B9 per_category_diagnostics, A8 manifold_history_fetcher — that VETO false edge claims but ran nowhere in CI; + fixed a confirmed RFC-8259 per_category NaN→None JSON hole). #259 A8 (the named STRUCTURAL research-only guardrail — a research_only tag on HistoricalMarket the real-money floor lane REFUSES; play money can no longer inflate the floor even by copy-paste; seed_hash 8dc358439ffb5746 unchanged, tag _seed_hash-excluded like category). #260 D4 (quarantine a legacy side="short" position — a BUY-on-short scaled it recording $0 PnL then trapped it; now rejected + warned). HONESTY FIX: corrected the 2026-07-07 Manifold "materially softer crowd" OVERCLAIM across ROADMAP/loop-memory/LOOP_HEALTH — ECE 0.027 is LOW (well-calibrated); the Brier-0.144-vs-~0.09 gap is the pinning(18.5% vs ~70%)+7d-lead confound, NOT worse calibration → a LESS-PINNED research corpus, NOT proven softer/beatable. LESSON: a reviewer's git-checkout on the SHARED tree stacked PR-2's commit onto PR-1's branch mid-work → FIX ADOPTED: spawn ALL reviewers with isolation:worktree. Closed stale bookkeeping PR #255 (its Manifold correction folded in here). No DoD/floor box. Binding constraint (business_case_strength B, no validated real-money edge) STANDS.
  _prev_run: 2026-07-07          # the PIVOTED-BINDING-CONSTRAINT run ("where is a crowd beatable?"), egress OPEN. Shipped 5 file-disjoint code PRs from a full 8-Haiku scout sweep (both deep-audit correctness + live-safety lenses SOUND — no CRITICAL/HIGH). #249 F11 (bootstrap significance CI on the tradeable OOS PnL/hit-rate — a green total is only an edge when its CI excludes 0; a Sonnet reviewer caught a real NaN→invalid-JSON bug on the insufficient_data path → fixed to None, 1 cycle). #252 A8 (Manifold PLAY-money research venue, reuses the audited _last_pre_decision_price guard; RAN LIVE → 2147 leakage-safe records, crowd Brier 0.144 / ECE 0.027 / 18.5% pinned — a LESS-PINNED corpus, NOT proven softer/beatable [corrected 2026-07-08: ECE 0.027 is LOW=well-calibrated; the Brier gap is the pinning+lead confound, not worse calibration]; ≥3 fresh Opus leakage auditors CANNOT-BREAK). #251 B9 (per-category crowd-calibration diagnostic; RAN LIVE n=1099 → Sports least-calibrated ECE 0.091, Politics sharpest 0.030; a SEARCH map, not an edge). #253 D2 (SELL/partial-reduce now feeds the per-strategy drawdown circuit — closed the named QUALITY_SCORECARD correctness A→A+ gap; no double-count, runtime harness still PASSES). #254 A3/§14 (an UNRECOGNIZED Kalshi settlement result logged LOUDLY, was DEBUG — makes the SELF_VALIDATION claim honest). 10 Sonnet reviewers all APPROVE (1 F11 fix cycle); 0 reverts. LESSON: a B9 branch got accidentally stacked on the F11 commit — caught via `git diff origin/default...HEAD --stat` before push, fixed with rebase --onto; new rule: verify the per-PR diff is disjoint BEFORE pushing. No DoD/floor box (research maps + integrity/correctness infra, no validated edge). Binding constraint (business_case_strength B, no robust real-money alpha) STANDS.
  _prev_run: 2026-07-05c         # 3rd run of 2026-07-05 — a GATED-LIVE-SAFETY-hardening run. Shipped 2 file-disjoint code PRs from a full 8-Haiku scout sweep (6/8 lenses NOTHING-GENUINE/all-DROP on the hardened engine), all 4 Sonnet reviewers first-pass APPROVE (1 pre-merge §5-runbook nit fixed), 0 reverts. (#241 D1/SIDE-EFFECT INTEGRITY, the headline) a 0-fill OPEN order fabricated a phantom Position(size=0) that POISONS the orchestrator token_id-in-positions dedup (orchestrator:1007) → later real opportunities on that token silently skipped + persists across restarts; is_success is True for OPEN so execute() called _update_position on a non-fill; paper-safe (_simulate_fill always FILLED); fix gates position/fee mutation on filled_size>0; 3 tests (2 fail-pre-fix). (#242 G/§28) refuse to boot a live host missing POLYMARKET_* venue creds — without them place_order silently REST-REJECTS every real order with no logger.critical (a live-but-broken bot); new _require_venue_credentials_in_live boot validator completes the live-boot safety triad; no-op in paper/CI; 4 tests (2 fail-pre-validator). B8 DEEPENED one level: the 254 Kalshi crypto series carry clean structured strikes (floor/cap_strike + strike_type), and the DECISIVE correction — quotes are ABSENT from the /markets list feed (0/254) but PRESENT in the /markets/{ticker}/orderbook endpoint (verified real depth on year-end KXBTCMAXY) → "no quotes" was really "wrong endpoint"; still multi-run/NOT built (speculative per DECISION COROLLARY). Scorecard functional_reality RAISED B→A by the independent Quality Auditor (#243, #165 confirmed fixed); business_case_strength=B is the lone binding constraint. No DoD/floor box; engine_pct 74.
  _prev_run: 2026-07-05b         # 2nd run of 2026-07-05 — a QUIET, coherent run: attacked the pre-registered #1 binding-constraint candidate (B8 cross-venue coherence) with a REAL RESOLVED-history feasibility probe BEFORE building → decisively confirmed + DEEPENED that B8 is a multi-run data-eng effort. PROBE: 500 resolved Polymarket by volume → 15 numeric-threshold (BTC/ETH TOUCH); 3000 settled Kalshi via general /markets → 0 clean single numeric-threshold binaries (KXMVECROSSCATEGORY multi-leg combos → matcher correctly refuses). Kalshi HAS 254 crypto series but reachable ONLY per-series with strikes in STRUCTURED cap_strike/floor_strike fields the title-text matcher can't parse, + a SEMANTIC mismatch (Kalshi KXBTCMAXY=barrier / KXBTCD=terminal vs Polymarket=touch) → NOT runnable this run, NOT built (speculative infra per DECISION COROLLARY). Shipped ONE genuine code PR (#238, A2/A5 — the ingest volume/liquidity finiteness guard: a 'nan'/'inf' volume slipped past the strategy BUY-gate filter since NaN<min_volume is False, the #165 ship-critical class; prices were already isfinite-guarded, this closed the asymmetry; 6 tests proven fail-pre-fix) from a full 8-Haiku scout sweep where 7/8 lenses were NOTHING-GENUINE / non-manifesting (verified: outcomes[0]=='Yes' 100/100, latent-tz doesn't change the pinned hash 8dc358439ffb5746, settlement sets closed_at atomically). 2 Sonnet reviewers first-pass APPROVE, 0 fix cycles, 0 reverts. QUALITY-RECONCILE: scorecard functional_reality=B (#165) already FIXED in code (#203 _volume_unavailable guard, stale scorecard, not open). No DoD/floor box; engine_pct 74. Binding constraint (business_case_strength B, no robust alpha) STANDS.
  _prev_run: 2026-07-05a        # the BINDING-CONSTRAINT PROBE + F10-completion run. Attacked the pre-registered #1 next candidate (B8 cross-venue coherence) on REAL live data BEFORE building: find_cross_venue_matches over 547 Polymarket + 200 Kalshi binary markets → 33 candidate pairings, ALL false + ALL below the 0.5 trade bar → ZERO tradeable matches; ROOT BLOCKER = the Kalshi /markets LIST feed returns NO usable quotes (all 200 active=False, 0.500 placeholder, garbled multi-outcome questions). So B8 does NOT validate this run (needs a real Kalshi quote source + a targeted numeric-threshold universe — a multi-run data-eng effort). The probe surfaced a REAL fix (#232). Shipped 3 file-disjoint PRs, all maker≠checker, 6/6 first-pass APPROVE, 0 reverts: #231 (F10/A2/A3/A6 — thread real per-market category END-TO-END so F10's category-concentration dimension fires on real OOS; category is _seed_hash-EXCLUDED metadata → pinned hash 8dc358439ffb5746 unchanged), #232 (B8 matcher active-gate — no fabricated 0.500 placeholder feeds a bogus cross-venue disagreement), #233 (gate test_llm_safety + test_persistence_rehydration_guard + the new category test, §15). No DoD/floor box ticked; engine_pct 74.
  _prev_run: 2026-07-04d        # 4th run of 2026-07-04 — the HF-UNLOCK + BUCKET-FAMILY-REFUTED run (egress open). Shipped 4 file-disjoint PRs, all maker≠checker (8 Sonnet reviewers + 2 delta re-reviews on #228): #226 (A6/OA-16 — remap the HF Polymarket-v1 fetcher to its CONFIRMED real daily_aligned schema → unlock the 1.3M-market corpus; ran it for real → 375 leakage-safe binary markets), #227 (D8/§32 — one bad position row no longer aborts the WHOLE rehydration → orphaned-positions safety), #228 (§14 — sweep the stale default-OPEN auth wording to default-CLOSED across config.py/DEPLOYMENT.md/render.yaml/routes.py), #229 (F10 — flag an edge concentrated in BOTH near-certain confidence buckets; the single-bucket check's blind spot). THE BINDING-CONSTRAINT WORK: ran the pre-registered static-B4a replication — a fresh same-day Gamma fetch (n=621) scored −$2,947 (3rd −/+/− sign-flip) AND the FIRST independent HF corpus (n=375) scored +$534 but FRAGILE (100% one horizon + 122% extreme-confidence). FOUR corpora, no robust positive ⇒ the price-bucket-calibration family (static+recency) is REFUTED/non-robust. Pivot to B8/B4 (structurally different). Binding constraint STANDS (now an alpha problem, not a data one).
  _prev_run: 2026-07-04c         # 3rd run of 2026-07-04 — the EGRESS-UNBLOCK run: the factory build env's own egress to gamma/clob/data-api/kalshi/huggingface confirmed OPEN (curl HTTP 200 + a real 799-record fetch). Shipped 5 file-disjoint PRs: #220 (A2 page Gamma at 100-row cap), #221 (B4a-revised recency alpha), #222 (B7 gate wallet_divergence OFF — data-api now reachable), #223 (UI Size col), #224 (E5/E6 timestamp drawdown). Recency-alpha OOS test REFUTED (−$914/n=799); static B4a +$3,330 vs −$2,938 Run 14 → sign-flipped.
  last_deep_audit: 2026-07-09b  # 8-Haiku scout sweep this run doubled as the deep audit (model/alpha correctness · backtest/leakage · risk/live-safety · data/venue ingest · learning/research-integrity · quality/tests-coverage · security/abuse · quality-reconcile+self-validation). Surfaced: the #275 confidence-units gap on 2 active strategies (alpha lens) + the NOPositionScanner residual + the #276 DQ-redundancy (correctly abandoned as churn). DROPPED-with-proof: backtest liquidity-passthrough (would activate the DORMANT uncalibrated impact-coeff placeholder + Gamma "liquidity" is $-notional not order-book DEPTH → dimensionally wrong), security /markets-auth (would 401 the frontend; a real inbound rate limiter is staged via the e2e_disable_rate_limit tripwire but needs frontend-under-limiter verification unavailable headlessly + is low-priority for a personal no-user bot), dead _category_exposure dict, whale_feed fabricated edge (gated), DQ price-NaN (MOOT #193), theater tests. Self-validation CLEAN (11 caps, unmet=[], declared==read, no stub-masquerade). Learning/security/quality-reconcile lenses NOTHING-GENUINE. PRIOR (2026-07-08 run "c"): 8-Haiku scout sweep this run "c" (data/venues · model/alpha correctness · backtest/leakage · risk/live-safety · learning/research-integrity · quality/tests-coverage · security/abuse · quality-reconcile+self-validation). Surfaced: the #267 dead forward-paper telemetry (learning lens), #268 NOPositionScanner edge-units (alpha lens), #269 boot-log secret leak (security lens, empirically reproduced), #270 unregistered live-path regression tests (coverage lens). Backtest/leakage lens NOTHING-GENUINE (engine reproduces bit-identically, fetchers refuse to fabricate). Dropped-with-proof: LogicalImplication undersizing (conservative), empty-strategy record_pnl skip (arguably-correct), 2 cosmetic data-layer nits. Quality-reconcile: scorecard stale on the (already-fixed) SELL/reduce gap + a drifted test count (non-ship-critical; Quality-Auditor-owned). EARLIER 2026-07-08 (runs a/b): 8-Haiku scout sweep (correctness · live-safety · leakage/overfitting · artifact-honesty · B8-nextstep · new-alpha · quality-reconcile · F-coverage). Correctness lens rated an orchestrator entry_price<=0 sizing mismatch CRITICAL but it is UNREACHABLE (DQV rejects out-of-range prices; the else-0.50 is a dead defensive branch) — NOT built (impossible-case). Live-safety surfaced the #260 BUY-on-short legacy quarantine. Leakage surfaced the #258 per_category NaN→JSON hole + the research_only structural-gap (#259). Artifact-honesty surfaced the Manifold overclaim (fixed this run). F-coverage surfaced the 3 ungated significance-net suites (#258). calibration.py/drift NaN = MOOT (API sanitizes), EXP-003 fetcher-category = STALE (post-#231), sklearn/cvxpy = imported (false positive). PRIOR (2026-07-05, 3rd run: data/venues · model/alpha · backtest/paper · risk/live-safety · learning/research · quality/integrity · security/secrets · quality-reconcile+self-validation) — Scout D surfaced the #241 phantom-position/dedup-poison bug; Scout G surfaced the #242 live-boot venue-cred gap; Scouts A/B/C/E/F/H NOTHING-GENUINE / all-DROP on the hardened engine (A latent+format-unverified; B's 3 non-manifesting — expected_value not read for Kelly, WeatherArb gated-off, FlashCrash skipped; E's E4/E7 unwired = architectural; F's bug_fixes.py dormant-experimental; H reconcile — functional_reality #165 already fixed, raised to A by #243). My own B8 orderbook-quote probe pinned the exact remaining data path. PRIOR (2026-07-05 2nd run): 8-Haiku sweep — B8 resolved-history probe + 7/8 NOTHING-GENUINE. PRIOR (2026-07-04): 6-Haiku sweep (data/alpha · risk/live-safety · correctness/dead-code · security/artifact · learning/egress-unblocked · backtest-integrity) — surfaced the §32 rehydration-orphan defect (Scout B), the regime_slice extreme-confidence blind spot (Scout F), and the stale auth-doc drift (Scout D); the alpha scout confirmed egress-open real-data work was the top lever (led to the HF remap + the decisive bucket-family refutation)
  enforced_in_ci: true          # required check (enforce_admins=true, strict=false) + repo auto_merge; loop merges MCP-squash/auto-merge on the green required check after reviews, WAITS for CI, never --admin
  validation:                   # self-validation capability readiness — refresh every run from `check_self_validation.py --readiness`
    enforced_in_ci: true        # the coverage gate is a blocking preflight step (9d) inside the required check
    capabilities_total: 11      # refreshed via `check_self_validation.py --readiness` this run "c" (unchanged — no new capability/credential added; declared==read, all validated).
    unmet: []                   # active + ci_validatable:false capabilities (need an owner secret). NON-EMPTY => urgent OWNER_ACTION + blocks. Must match SELF_VALIDATION.readiness.unmet AND have a PENDING_OPS validation-capability-<id>.
  this_run:
    changes_shipped: 1          # 1 file-disjoint code PR (#275 B/correctness: pin ScanResult.confidence to the reconstructed win_probability=entry+edge on the 2 ACTIVE trade-executing default-scan strategies — CrossMarketArbitrage + LogicalImplication) + this bookkeeping. maker!=checker, 2 worktree-isolated Sonnet reviewers (A first-pass APPROVE; B took 2 HONESTY cycles — the FIX was correct each time, my "last two remaining" completeness CLAIM overclaimed; resolved by dropping completeness language + enumerating all 7 default strategies). Low count is HONEST anti-padding on a heavily-mined engine.
    _prev_run_shipped: 1        # 2026-07-09 (live-safety): #272 live fills charge the venue fee so the hard loss caps net the exit fee. maker!=checker, 2 reviewers APPROVE, 1 nit fold-in; no DoD box.
    changes_abandoned: 1        # #276 (A5 DQ finite-check) ABANDONED as review_value: a CORRECT isfinite check but REDUNDANT with #238's parser-level guard (both live parsers already isfinite-guard volume/liquidity; the gate-layer duplicate can never fire on real data = churn, the #193 redundancy class). Value-first Reviewer B caught it; verified the parser guards directly, closed the PR. 0 abandons of MERGED work; 0 reverts.
    abandoned_reasons: ["2026-07-09b (units-contract): (a) #276 DQ volume/liquidity isfinite check BUILT+reviewed then ABANDONED (review_value) — a CORRECT check but REDUNDANT with #238's parser-level guard (polymarket_client._parse_market `if math.isfinite(fv) and fv>0` :845/:858; kalshi_client._to_float returns None for non-finite :340; both in base), and no other live path builds a Market with non-finite volume/liquidity, so the gate-layer duplicate can never fire on real data = churn (the #193 price-NaN redundancy class). (b) backtest liquidity-passthrough DROP: the impact term (DEFAULT_IMPACT_COEFF=0.5) is a DELIBERATELY-DORMANT uncalibrated placeholder AND Gamma 'liquidity' is $-notional not order-book DEPTH → feeding it to effective_buy_price_with_impact(...,depth) is dimensionally wrong. (c) security /markets,/search,/weather auth DEFERRED-as-designed: /markets is called UNAUTHENTICATED by the frontend (predictions/page.tsx:216) so _MUTATING_AUTH would 401 the UI; a proper inbound rate limiter (the real §12 fix, config e2e_disable_rate_limit tripwire staged) needs frontend-under-limiter verification unavailable headlessly + is low-priority for a personal no-user bot. (d) NOPositionScanner confidence = RESIDUAL tracked to ROADMAP B (fixing it disables the strategy — a design decision, not a units fix). (e) dead _category_exposure, whale_feed fabricated edge (gated), DQ price-NaN (MOOT #193), theater tests = DROP. 2026-07-09 (live-safety): (a) ingest NaN-guards (data-scout, 7 findings: whale_feed price/size/pnl/amount, noaa_weather temp/precip, polymarket_client tick_size/spread) DROP: verified via orchestrator _build_default_scanner that WhaleCopyTrading+WalletBehaviorDivergence+WeatherArbitrage are ALL gated OFF behind ENABLE_UNVALIDATED_STRATEGIES (default off; wallet #222), and OrderBook.tick_size/min_order_size are never read + get_spread() never called (dormant) -> fixing invisible NaN math on gated-off/dead code is churn (same class as the prior WeatherArb-formula DROP), NOT the #165 ACTIVE-path class. (b) NOPositionScanner confidence=min(adjusted_rate*2,0.95) (model-scout, advanced_strategies.py:265) DROP: the scout fix (confidence=adjusted_rate) is self-contradictory (adjusted_rate<=0.50, typically <<0.50, so it gates off MORE signals at the 0.50 min_confidence pre-filter, not fewer); NOPositionScanner is a LOW-prob reversal strategy (win_prob<0.5 by design) so the *2 is a deliberate hack to lift some signals over a gate that structurally conflicts with the thesis; the correct fix would make an UNVALIDATED strategy trade MORE (off-thesis, risky) -> ambiguous design + wrong fix, do not ship. (c) risk-scout finding 2 (add fees to risk_manager.record_pnl) SUBSUMED: the #272 source fix (populate result.fees on the live fill) automatically feeds the existing record_execution _daily_pnl-=result.fees; a separate record_pnl fee arg would DOUBLE-count. (d) backtest/leakage, learning/research-integrity, security/abuse, self-validation, quality/tests-coverage lenses all NOTHING-GENUINE/clean."]
    verify_cycle_failures: 0     # #275 preflight code GREEN on every attempt (local, incl. after each docstring amend). No CI/gate fix-cycle; the changes across cycles were CLAIM-honesty narrowing, not gate failures.
    review_rejections: 3         # HONEST: Reviewer A APPROVED #275 + #276 first-pass. Reviewer B gave 3 REQUEST_CHANGES: 2 on #275 (both HONESTY — my "last two remaining" completeness claim overclaimed; it found MarketMaking then NOPositionScanner; the FIX was correct each time, only the CLAIM was wrong; resolved by dropping completeness language + enumerating all 7 default strategies, within the ≤2-review-cycle cap — cycle 2 implemented B's own pre-authorized option b), and 1 on #276 (correctly rejected as redundant churn → ABANDONED, not fixed). The value gate WORKING: it caught an honesty overclaim on a correct fix AND killed a redundant PR.
    process_incidents: 1         # I ran `rm -rf .claude/worktrees` + `git worktree prune` while a worktree-isolated Reviewer B subagent was STILL running, disrupting its worktree (it recovered via direct file reads and completed the review). LESSON: only prune worktrees AFTER all agents complete. No impact on shipped correctness.
    circuit_breaker_trips: 0     # no command failed the same way twice; no diff stopped changing; the 2 #275 review cycles converged (each narrowed the claim to a verified-complete enumeration), not a stuck loop.
  rolling_7d:
    merged_prs: 165            # +2 this run 2026-07-09b (#275 + bookkeeping; #276 built-then-abandoned, not merged); +1 the 2026-07-09 live-safety run (#272 + bookkeeping #273); +5 the 2026-07-08c run (#267 + #268 + #269 + #270 + bookkeeping). loop-cadence count — the true 7d repo total is higher (FACTORY_STANDARD-sync, Quality Auditor + research routines incl. #262/#274 in the same window)
    reverts: 0
    readiness_attempts: 0
    readiness_rejected: 0
    recurring_failures: []       # The VALIDATED-OOS-EDGE binding constraint (business_case_strength B) STANDS — an ALPHA problem, not a code one (egress open; bucket family refuted across 4 corpora; B8 pinned but a multi-run build; Manifold/Kalshi crowds sharp). This run (2026-07-09b) shipped ONE durable correctness PR (#275) + correctly abandoned one redundant candidate (#276); NOT `stuck` — the 3 review-rejections were HONESTY-narrowing on a correct fix (2×) + a correct kill of churn (1×), not a wall on a broken change; the 1 process incident (worktree prune mid-review) produced a durable lesson. PRIOR: run "c" shipped 4 durable correctness/integrity/security/coverage PRs (0 reverts, 8/8 first-pass APPROVE, 1 CI fix-cycle resolved) — the engine keeps getting more honest/correct while the alpha search continues in the research lane. NOT `stuck`: each run attacks the constraint or hardens the machine from a different angle; the 1 CI fix-cycle (a light-gate fastapi-dep miss) was caught by the deterministic gate and produced a durable lesson, not a wall. NOTE (prior runs, still true): the factory build env's egress to gamma/clob/data-api/kalshi/manifold/huggingface is OPEN — OA-11/13/16 are not loop-blocked; the constraint is "no alpha has a validated robust real-money edge yet", not data access. PRIOR (3rd of 2026-07-05) hardened the gated-live path (2 PRs) and DEEPENED the B8 data-path finding one level (structured strikes confirmed parseable; quotes confirmed reachable via the /markets/{ticker}/orderbook endpoint, not the list feed) — still a multi-run build, NOT built (no validated universe to exercise it against yet → speculative per DECISION COROLLARY). This is NOT `stuck` (0 reverts, 0 shipped abandons, 2 durable PRs, 4/4 first-pass APPROVE; each run attacks the constraint from a different angle — bucket family refuted across 4 corpora, B8 probed 3× and its exact data path now pinned): the loop is converging on WHAT it takes to validate an edge, not spinning the same wall. PRIOR NOTE: the factory build env's own egress to gamma/clob/data-api/kalshi/huggingface is NOW OPEN (self-confirmed HTTP 200 + a real 799-record fetch), so OA-11/13/16 are no longer loop-blocked. The recency-alpha OOS test RAN (n=799) and REFUTED (−$914); the static B4a sign-flipped across corpora (+$3,330 vs −$2,938 Run 14) = not a robust edge. So the constraint is now "no alpha has a validated robust edge yet", NOT "can't reach data". NOT trending `stuck` (0 reverts, 0 shipped abandons, 5 durable PRs, 1 resolved review cycle). live-validation.yml + real-oos-validation.yml green.
    harness_proposals_open: 0
  signal: steady                 # 42nd datapoint: 2026-07-09b a UNITS-CONTRACT correctness run on the mature engine. The full 8-Haiku scout sweep surfaced ONE genuine value-bar-clearing item — #275 (pin ScanResult.confidence to the reconstructed win_probability on the 2 active trade-executing default-scan strategies, closing the #263/#268 units-contract gap so the min_confidence safety gate filters on the true fair value). Shipped it (2 worktree-isolated Sonnet reviewers; A first-pass APPROVE, B took 2 HONESTY cycles that correctly caught + fixed a "last two remaining" completeness OVERCLAIM — the fix was always correct, the claim wasn't; resolved by enumerating all 7 default strategies). BUILT then correctly ABANDONED #276 (review_value: a redundant duplicate of #238's parser guard = churn) — the value gate WORKING, not churning. Tracked the NOPositionScanner residual to ROADMAP B (fixing it disables the strategy — a design call). STEADY: 1 shipped / 1 abandoned-pre-merge / 0 reverts / 0 shipped-abandons; the abandon is anti-padding discipline, not thrash; the 2 review cycles were honesty-narrowing on a correct fix, not a broken one. Binding constraint (business_case_strength B, no validated real-money OOS edge) STANDS, owned by the sibling research routine. PRIOR — 41st datapoint: 2026-07-09 a LIVE-SAFETY run on the mature engine. The full 8-Haiku scout sweep surfaced exactly ONE genuine value-bar-clearing item (#272: live fills now charge the venue fee so the hard loss caps net the exit fee, closing a paper/live asymmetry that undercounted real cash loss on the gated live path). Shipped it through both gates (2 worktree-isolated Sonnet reviewers APPROVE, 1 reviewer-nit fold-in, 0 REQUEST_CHANGES, 0 reverts), dropped every other finding PRE-build with recorded proof (gated-off/dead-code churn, a self-contradictory strategy-confidence fix, a would-be double-count). STEADY not improving: shipped-count fell 4->1, but that is HONEST scarcity on a heavily-mined engine (anti-padding), not churn: 0 abandons of shipped work, 0 reverts, converging. Binding constraint (business_case_strength B, no validated real-money OOS edge) STANDS, a research problem owned by the sibling routine.
```

## How to read the latest signal

**2026-07-04 (4th run) — `improving`, the HF-UNLOCK + BUCKET-FAMILY-REFUTED run: 4 file-disjoint PRs, all maker≠checker, 0 reverts, 2 converging review cycles (both #228).**
With egress open, the factory streamed the HuggingFace `Polymarket-v1` `daily_aligned` layer for the first time, CONFIRMED its real (per-trade/per-outcome) schema, and remapped the fetcher to it (**#226**) — the one load-bearing choice being P[YES]←`p_event` with raw `price` excluded (it's the traded-leg price and would invert every 'No' row). Ran it end-to-end → **375 leakage-safe binary markets** assembled from the 1.3M archive. **THE BINDING-CONSTRAINT WORK:** ran the pre-registered static-B4a replication — a fresh same-day Gamma corpus (n=621) scored **−$2,947** (the third −/+/− sign-flip), and the first genuinely-independent corpus (HF, n=375) scored **+$534.88 but FRAGILE** (100% one horizon + 122% extreme-confidence). Four corpora, no robust positive ⇒ the price-bucket-calibration family (static + recency) is **REFUTED / non-robust**. **The decisive discipline:** I DROPPED the pre-registered rolling-window bucket variant as padding — a 3rd variant on a refuted family is iterating on noise; the honest weak-case conclusion is **pivot** (B8 cross-venue coherence + B4 deep research). Running the data FIRST turned a would-be padding PR into a decisive refutation. Plus **#227** (§32 rehydration-orphan safety), **#228** (§14 auth-doc default-CLOSED sweep — Reviewer B caught 2 more stale comments across 2 converging cycles), **#229** (F10 extreme-confidence fragility blind spot, which validated F10 end-to-end on the #226 fragile result). No DoD/floor box ticked. NOT churning/stuck → no harness proposal.

**2026-07-04 (3rd run) — `improving`, the EGRESS-UNBLOCK + weak-case-loop-back run: 5 file-disjoint PRs, all maker≠checker, 0 reverts, 1 resolved review cycle.**
THE STATE CHANGE: the factory build env's OWN egress to `gamma/clob/data-api/kalshi/huggingface` is **now open** — self-confirmed by direct `curl` HTTP 200 to all five + a real **799-record** leakage-safe fetch through the committed pipeline. Every prior run treated this as owner/egress-blocked (OA-11/13/16); Research Run 14 (a different session) found egress open in its env and asked the factory to re-probe from its OWN env — I did, and the loop can now fetch real data itself. Shipped: **#220** (A2 — page Gamma at its real 100-row cap; the pager stopped after 1 page, under-sampling every corpus — the root cause corpora topped out near 100 records), **#221** (B4a-revised — a NEW recency-weighted bucket alpha, the pre-registered first-principles follow-up to the refuted static EXP-002), **#222** (B7/safety — gate `wallet_divergence` OFF: its fabricated-edge data-api feed is now reachable → executed OA-13's pre-registered remediation, the mirror-of-#215 evidence-backed reversal), **#223** (UI — positions "Size" shows contract count not notional $), **#224** (E5/E6 — order intra-window trades by full timestamp for correct drawdown). **THE BINDING-CONSTRAINT WORK (§9 weak-case loop-back):** ran the pre-registered recency-alpha OOS test ONCE on the real n=799 corpus — **REFUTED** (−$914/108tr, worse than the static variant); and the static B4a was **+$3,330 here vs −$2,938 in Run 14 (n=510)** → sign-flipped across corpora = **not a robust edge**. Binding constraint (no validated OOS edge) **stands**, but its blocker is now "no robust alpha yet", not "can't reach data". No DoD/floor box ticked. NOT churning/stuck → no harness proposal.

**2026-07-03 (4th run) — `improving`, a QUIET coherent run: ONE code PR (gated-live order-path integrity) shipped; the scout sweep was mostly false-positives I verified + dropped with proof; the adversarial gate caught a None-crash MY fix introduced, and a §28 skip-to-green trap killed a 2nd PR.**
An 8-Haiku scout sweep across tracks A–G surfaced ~10 candidates on the mature, hardened, egress-blocked engine — but
only ONE cleared the value bar honestly. **#199 (D6/D1/§12 — gated-live order-path integrity):** two real defects on the
gated-live Polymarket order path (unreachable in paper: `_place_via_rest`/`_place_via_clob_client` require
`is_authenticated`; paper uses `_simulate_fill`) fixed to build the live path safe-by-default: (a) the REST FILLED path
OMITTED `filled_price` → a live REST fill defaulted `avg_entry_price=0.0` → realized PnL = settlement×size (all gain, no
cost basis) + entry fee 0 → the hard loss caps / kill switch would gate on inflated net PnL (the CLOB sibling + the paper
sim both set it; REST was the outlier); now both live paths set `filled_price = req.price or 0.50` (the EXACT effective
limit each submits at, never None); (b) `error=str(e)` at both exception handlers leaked venue host:port to the
`/prediction-markets/execute` HTTP response → `type(e).__name__` (§12, mirroring `routes.py::_safe_conn_error`). **THE
HEADLINE (discipline):** the adversarial gate caught a None-crash MY OWN first cut introduced — Reviewer A reproduced
end-to-end that `req.price=None` (schema-valid for GTC/FOK) → `filled_price=None` → a downstream `market_value` TypeError
AFTER a real order was placed (worse than the old crash-safe 0.0); fixed at the source with `req.price or 0.50` (the exact
submitted limit) + a None-price fail-pre-fix test → fresh correctness re-review APPROVE. **§28 catch:** abandoned a
would-be 2nd PR (register `test_backend_auth_fastapi` in the blocking gate) because it `importorskip`s fastapi, absent from
the deliberately-light `requirements-ci.txt` → gating it would SKIP-to-green (validate nothing); the other 15 unregistered
tests mostly target DELETED stock-era modules. **Anti-padding held hard** (cross_venue no_p code is correct/scout misread;
walk_forward impact-sizing "fix" breaks a deliberate invariant; NearCertainty p>1 is Kelly-clamp-harmless; tz-naive utcnow
is DB-normalized; Kalshi flat-tick is fixtures-only; the dead-looking persistence API is deliberately-kept). **No DoD/floor
box ticked** — VALIDATED-OOS-EDGE stays owner/egress-blocked (OA-11/15/16). NOT churning/stuck (0 reverts, 1 resolved fix
cycle, 1 §28-proven pre-build abandon) → no harness proposal.

**2026-07-03 (3rd run) — `improving`, the DEFERRED SAFETY FIX landed: loss-cap net-of-fees (D3/D4) w/ a fresh Opus live-safety audit + 4 more file-disjoint PRs; 5 shipped, 0 reverts.**
An 8-Haiku scout sweep across tracks A–G selected the maximal file-disjoint value-bar-clearing set. The headline was the
**loss-cap NET-OF-FEES safety fix (#192)** the 2nd run VERIFIED-and-deferred for a dedicated run: the hard loss caps +
kill switch gated on the GROSS price move while the ~2%-notional venue fees (tracked only in a reporting-only field) were
never subtracted, so the cap undercounted the true cash loss — a position could lose more real cash than the owner's
ceiling before the kill switch tripped. `record_realized_pnl(pnl, fees=)` now nets fees so the caps gate on true net cash
PnL (resolution nets the exact entry fee; reduce nets exit+entry), which can only ever trip the cap EARLIER (conservative).
2 Sonnet APPROVE + a **fresh Opus live-safety auditor SAFE** (verified conservation exactly and that 9/10 money-proof
tests fail on pre-fix code). Alongside: **#193 A2/A5** (CLOB `get_price`/`get_midpoint` validated to [0,1] at the source
like the WS `_coerce_prob` — a NaN poisoned `FlashCrashStrategy`'s rolling buffer after the market-level validator; the
sibling `data_quality` gap was honestly dropped as moot since `check_completeness` already rejects it), **#194 §10** (dead
`advanced_weighting_engine`, 432 lines / 0 importers, deleted), **#196 D7/§12** (bounded the last unbounded state-mutating
param, the `exchange` query on the cancel route), and **#195 F1/F7** (registered `test_audit_log` + `test_loss_cap_fees`
in the blocking gate — the single shared-resource edit). **THE HEADLINE (process): I branched every PR from a STALE local
base ref** (before #187–191 merged) — harmless for the 4 file-disjoint PRs (clean squash onto the real base) but #196
(routes.py) re-added #188's already-merged bounds → a conflict + a false "#188 not an ancestor" claim; **both #196
reviewers caught it**, I `git merge-base`-verified against `origin`, rebuilt #196 minimal (only the genuinely-missing
`exchange` bound) → 2×APPROVE. Lesson: hard-reset the local base ref to `origin` before branching; a PR-body ancestry
claim is checkable — verify it against `origin`, not a local ref. **Governance win:** #192 Reviewer A's "the safety net
isn't in CI" REQUEST_CHANGES was resolved via a dedicated gate PR (#195, §15 one-shared-resource-edit) + a §15-armed
re-review (the #149 pattern), not by putting a shared-resource edit in a code branch. **Anti-padding held** (dropped
tz-naive `utcnow` — no reachable bug on Py3.11; `backtest/metrics` degenerate-metric "fixes" — off the PM path + `inf`
breaks JSON; config residue; a declared-capability deletion; frontend nits). **No DoD/floor box ticked** — the
VALIDATED-OOS-EDGE binding constraint stays owner/egress-blocked (OA-11/15/16). NOT churning/stuck (0 reverts, 1 resolved
fix cycle) → no harness proposal.

**2026-07-03 (2nd run) — `improving`, a mature-engine HARDENING sweep: 4 file-disjoint PRs, all maker≠checker first-pass 2×APPROVE (0 fix cycles / 0 abandons / 0 reverts).**
An 8-Haiku scout sweep across tracks A–G selected the maximal file-disjoint value-bar-clearing set on the mature,
egress-blocked engine: **#187 A2** (a WebSocket `price_change` staleness-honesty guard — stop refreshing/caching a
stale quote on an all-invalid book update, mirroring the sibling `last_trade_price` guard; the in-memory cache feeds
live strategies + executor sizing), **#188 D7/§12** (bounded the last two unbounded state-mutating PATH params —
`cancel/{order_id}`, `risk/enable-strategy/{strategy_name}` — completing the input-bounds pattern; the latter also a
log-injection vector), **#189 F7** (api/main.py import hygiene: a dead import + a redundant local re-import + 5× E402
→ ruff-clean, the literal module-by-module F7 unit), and **#190 §10** (deleted two grep-proven-dead functions). THE
HEADLINE — I VERIFIED the risk scout's highest-stakes finding (the hard loss cap / kill switch tracks GROSS realized
PnL and never subtracts the ~2%-notional fees, so it undercounts true cash loss — real, traced at both call sites) but
DEFERRED it rather than churn a stable, A-graded, guard-tested safety anchor inside a multi-PR sweep; it's paper-only
+ small-magnitude and belongs in a dedicated run with a fresh Opus live-safety audit, recorded with exact file:lines.
No DoD/floor box ticked — the VALIDATED-OOS-EDGE constraint stays owner/egress-blocked (OA-11/15/16).

**2026-07-03 (1st run) — `improving`, B8 cross-venue coherence matcher (the 2nd, more-robust alpha CANDIDATE) + F10 wiring + gate coverage; the adversarial gate broke B8 FOUR times and I ended the rabbit hole by ELIMINATING the fragile heuristic.**
An 8-Haiku scout sweep across tracks A–G selected the maximal file-disjoint value-bar-clearing set on a mature,
egress-blocked engine: **B8** (#179 — the cross-VENUE coherence edge matcher + cost-net backtest, the lowest genuinely-
incomplete NEW-capability item: trade the price gap when the SAME event is priced differently on Polymarket vs Kalshi,
a logical-consistency edge that does NOT require out-calibrating the crowd), **F10** (#180 — wired the built-but-uncalled
`analyze_regime_slices` into the real-OOS lane + fixed a structurally-false category FRAGILE at the `regime_slice` root),
and **gate coverage** (#182 — registered the #156 confirmed-outage test `market_category` [it was NOT in the required
gate] + `weekly_metrics` + the B8 test). **THE HEADLINE — when an adversarial finding RECURS in the same sub-system,
ELIMINATE the fragile mechanism, don't keep tweaking it.** B8's numeric-strike direction parsing was broken by the gate
FOUR times across 3 fix cycles (a 2% adjacent-strike tolerance; a no-threshold pair reaching the trade bar; word-form
then contraction negation; and — the tell — a NEW spurious cross-clause negation inversion that MY cycle-2 fix
introduced). The escape was not a smarter heuristic (which spawned the regression) but DELETING inversion entirely: a
negated comparator now VOIDS the strike, which is TIGHTENING-ONLY — a voided strike can only cause a conservative
reject/no-trade, never a fabricated match, so the whole class of inversion edge-cases is structurally impossible. 2
Sonnet reviewers + 4 Opus adversarial audits + a final Sonnet confirmation (tightening-only holds, no tradeable false
match). **Anti-padding held:** ABANDONED A3 (Kalshi category routing) after the value reviewer proved it re-does a
loop-dropped nit on dormant, non-live-scanned Kalshi code — the correctness APPROVE of the code did not override the
value proof. **No DoD/floor box ticked** — the VALIDATED-OOS-EDGE binding constraint stays owner/egress-blocked
(OA-11/15/16); B8 adds a second, possibly-more-robust alpha CANDIDATE (gated matcher+backtest infra) for when real
dual-venue corpora exist. NOT churning/stuck (0 reverts, 1 correct anti-padding abandon, 3 durable PRs; B8's 3 fix
cycles CONVERGED on a structurally-safe design) → no harness proposal.

**2026-07-02 (4th run) — `improving`, the DATA/INTEGRITY-cluster run: 5 file-disjoint PRs advanced the lowest-incomplete loop-buildable items attacking the binding constraint; the adversarial gate broke 2 and I fixed through it.**
An 8-Haiku scout sweep selected the maximal file-disjoint set of lowest-incomplete, loop-buildable items that
attack the binding constraint (a validated OOS edge): **A6** (#168, the HuggingFace Polymarket-v1 fetcher — the
1.3M-market VOLUME unlock), **A7** (#169, point-in-time / earlier-life sampling — the edge-HEADROOM unlock for the
70%-pinned corpus), **A3/OA-15** (#170, the live-verified Kalshi candlesticks `/history`-404 fix), **F10** (#171, a
pure `regime_slice.py` anti-overfitting report — slice OOS PnL by category/horizon/confidence/time + a concentration
map + a `fragile` flag), and a **kelly_size non-finite guard** (#172). **The adversarial Opus gate earned its keep
HARD:** it BROKE 2 of the 5 on real bugs the 2-Sonnet review + my own tests passed over — A3 (a 1¢ nested candle
normalised to a fabricated `p=1.0`, the data analog of a fake fill) and A6 (a `max(resolution_time)` jittered-
resolution LEAK, then a null-outcome survivorship hole) — and each was fixed THROUGH the gate within the ≤2-cycle
brake (A6 used both cycles → a FINAL re-audit returned FIX-HOLDS → merged). The cross-cutting NaN-timestamp scout find
was FOLDED into each owning PR (A7/A3/A6), not a 4th colliding PR. The kelly scout's "NaN edge → NaN order" headline
was partly inaccurate — the 3 REAL leaks (confidence/bankroll) were verified before shipping. Anti-padding held
(dropped the double-protected FK defense-in-depth). **No DoD/floor box ticked** — the VALIDATED-OOS-EDGE binding
constraint stays owner/egress-blocked (OA-11/15/16), but this run built BOTH orthogonal unblocks (A6 volume + A7
headroom) + the A3 second-venue contract fix + F10 the net that catches a fragile edge. NOT churning/stuck (0
reverts/abandons, 5 durable PRs, 3 fix cycles all resolved through the gate) → no harness proposal.

**2026-07-02 (3rd run) — `improving`, UNFROZE the live forward loop: a loop-buildable production fix found in the real CI logs, plus FK-coverage hardening + artifact freshness (3 PRs).**
The highest-EV start was NOT a cold scout sweep — it was VERIFYING the independent Research Run 12's two URGENT
cross-routine flags against the **real** live GitHub Actions logs. The scheduled OA-17 forward-paper cycle had gone
**dead in the water**: the latest run skipped **154/154** opportunities on `Category 'General' exposure: $164.18 +
$50.00 > $200.0`. Root cause (observed, not theorized): real Polymarket Gamma markets ship an **empty** `category`, so
`risk_manager` bucketed every market as `"General"` and the per-category $200 cap became a de-facto **global** cap below
the real $500 portfolio cap — no trade of any theme could execute. Shipped **3 file-disjoint code PRs + this
bookkeeping**, all maker≠checker (8 reviewers — 2 Sonnet/PR + 1 Opus adversarial auditor on the two code PRs — **ALL
APPROVE/SAFE/SOUND on the first pass**): **#156** the LIVE-FREEZE fix — a new pure/deterministic `market_category.py`
derives a coarse real correlation bucket (Crypto/Politics/Sports/…) from tags + a punctuation-insensitive keyword scan
of the question; the parser stamps it; `Position` carries the category so a position rehydrated across the
fresh-process cycle counts against its real bucket, not `"General"`. The Opus safety auditor **SAFE** — verified the
$500 global backstop still binds at BOTH the risk-manager and executor layers, correlated themes stay in one bucket, and
no free-text path can create a per-market unlimited bucket. **#157** hardened the **live** order-persist path
(`orchestrator._persist_order` — the real writer; `persistence.save_order` is dead code) with an in-transaction
`_ensure_default_portfolio` + an **FK-enforced** regression test (`PRAGMA foreign_keys=ON`) that closes the
SQLite-FK-blind coverage gap on the live writer. The Opus side-effect auditor **SOUND** + independently reproduced the
baseline (the test fails on pre-fix code). **#158** artifact freshness — `render.yaml`/`database.py`/`requirements`
Supabase→Neon + `ANTHROPIC_API_KEY`→`GEMINI_API_KEY` (the wrong key name was a no-op that would misdirect the owner) +
deleted the dead root `test_alternative_data.py`. **THE PROCESS WIN — verify a cross-routine flag against the REAL
system before building.** Research Run 12 flagged two urgent items; one (the category freeze) was live + loop-fixable →
shipped; the other ("FK persist still fails") was **disproven as a live bug** — its 6/6-failure evidence was from a run
BEFORE the #140 seed landed, and current live runs rehydrate $164 of positions (impossible if the path still FK-failed)
— so instead of re-fixing a non-bug, we shipped the genuine gap it exposed: a missing FK-enforced test on the live
writer. **Anti-padding held:** dropped a volume=inf finitude guard (low reachability, not demonstrable on real data,
file-collides), the executor SELL-floor/SHORT-PnL nits (unreachable — SELL/short never routed), and the sub-2¢ impact
(deliberate C3 deferral). **No DoD/floor box ticked** (an operational unfreeze + correctness/coverage/freshness, not a
validated edge). The VALIDATED-OOS-EDGE binding constraint stays owner/egress-blocked (OA-11/15/16). NOT churning/stuck
→ no harness proposal.

**2026-07-02 (2nd run) — `improving`, the big A→A+ convergence run: 5 file-disjoint PRs cleared BOTH remaining ship-critical correctness A→A+ gaps + a real parse fabrication + dead code.**
An 8-Haiku scout sweep across tracks A–G surfaced the maximal file-disjoint, value-bar-clearing set for a mature,
egress-blocked engine — QUALITY / CORRECTNESS / HONESTY convergence, not new completeness. Shipped **5 code PRs +
this bookkeeping**, all through maker≠checker: **#149** F7 — a CORRECTNESS-only ruff gate (`--select E9,F821,F811`)
now ENFORCED in the required CI (+ `ruff` in requirements-ci.txt; the 3 pre-existing F811 cleared), verified GREEN in
the real check before merge; **#150** artifact freshness — 4 dead root deps + the dead `AUTO_CONNECT_BROKERS` env var
scrubbed from 3 files + stale README architecture; **#151** A1 — 1055 lines of dead stock-era code deleted
(`advanced_validators.py` + `monitoring/monitoring.py`, 0 importers); **#152** parse honesty — a missing
`outcomePrices`/`outcomes` field no longer fabricates a tradeable market (the residual key-level gap #101's
index-level fix missed; the Opus auditor's named sibling-`outcomes` incompleteness folded in); **#153** THE SQLModel
dual-import fix deferred ~4 runs — 30 test files standardized `backend.app.*`→`app.*`, killing the DeclarativeMeta
double-registration, PROVEN by a baseline reproduction (base = 3 order-dependent failures + SAWarning → branch =
1168 passed / 0 failed / 0 warnings, order-independent across 3 pytest-randomly seeds). **The gate + governance both
earned their keep:** the SQLModel sed broke a subprocess sys.path (caught by preflight before push) and corrupted 3
bug-describing docstrings (caught by Reviewer B); #149's Reviewer B REQUEST_CHANGES (edit ROADMAP/scorecard in the
code PR) conflicted with §1/§8/§15 and was resolved by a governance-armed re-review + doing the ROADMAP F7 update
HERE in bookkeeping. Anti-padding held (dropped a cosmetic `expected_value` nit, a deliberately-deferred `reconcile()`,
a known D2 accuracy follow-up; 2 lenses NOTHING-GENUINE). No DoD/floor box ticked; the binding constraint stays
owner/egress-blocked (OA-11/15/16 all 403). NOT churning/stuck → no harness proposal. The two ship-critical A→A+
top_gaps (ruff/artifact + SQLModel correctness) are now resolved for the Quality Auditor's next pass.

**2026-07-02 (1st run) — `improving`, shipped the D8 forward-record-coherence headline the owner-directed audit filed.**
The prior owner-directed audit (#141) found the scheduled paper cycle runs as a fresh process each run and
NEVER rehydrated open positions, so positions opened in a prior run were orphaned in the DB and `check_resolutions`
never settled them (realized PnL never booked, the forward record couldn't progress), plus no scan dedup →
double-counting. Filed as ROADMAP D8 "for the factory to fix through the two-gate adversarial review." This run
did exactly that: **#143 (D8)** landed the coupled fix (open-only rehydrate + `get_executor` wiring + scan
`skip_held` dedup) — and building the regression suite surfaced a genuine LATENT bug the zero-call-site helper hid
(a `DetachedInstanceError` because `load_positions_into_executor` read ORM attributes after the session closed),
fixed by constructing positions inside the session. 6 of 7 tests proven to fail on pre-fix code; 2 Sonnet reviewers
APPROVE + a fresh Opus safety auditor SAFE (verified no double-settle, kill-switch/loss-caps untouched, no fabricated
fill). Shipped alongside **#144** (§12 bounds on 3 unbounded API params — the strongest being `bot/start`'s
`scan_interval_sec=0` tight-loop) and **#145** (dead ML deps out of the root requirements.txt + a stale DEPLOYMENT.md
Vercel claim). The gate earned its keep on #145: both deploy reviewers caught a false "render.yaml installs the root
file" justification (it uses `backend/requirements.txt`), fixed in one cycle. Anti-padding held — dropped a
load-guarded frontend `|| 0` and a gated-off B7 whale bug, deferred the SQLModel dual-import + ruff A→A+ polish as
out-of-scope. No DoD/floor box ticked; the D8 forward record can now progress but the validated-OOS-edge box is
unchanged (owner/egress-blocked corpus + a real alpha remain the binding constraint). NOT churning/stuck → no harness
proposal.

**2026-07-01 (owner-directed) — `improving`, added the "real" (live) self-validation tier: mock → real.**
Built the second validation tier the required deterministic gate deliberately can't be: **(1)**
`scripts/live_integration_smoke.py` — a real Gemini call (when `GEMINI_API_KEY` is set) + a real public
Polymarket read, with honest exit semantics (fail only on a genuine *code* break; external
unavailability is reported, never red); **(2)** `scripts/run_paper_cycle.py` — a **forward paper-trading
cycle on REAL live markets** ("live validation with paper money"): scan real open markets → record
as-if-filled (cost-aware, **no venue call**) → book realized PnL on resolution. Doubly safe — refuses to
run if `LIVE_TRADING_ENABLED` is true AND uses the dry-run executor (verified: config *and* runner both
refuse). Added `paper_trading_forward` to the manifest (11 capabilities, `unmet=[]`); `llm_analysis` +
`polymarket_market_data` are now **dual-validated** (mock in the required gate + real in the non-blocking
smoke). 6 deterministic safety/classification tests added to the required gate; the real tier is staged
as a **non-blocking** scheduled workflow (`docs/ci/PROPOSED_LIVE_VALIDATION.md`, **OA-17**) so a Gemini/
Polymarket outage can never freeze merges. **No trading key ever enters CI** (real orders stay human-core;
the live path stays validated-as-gated-off). Full gate green. Same binding constraint (real OOS corpus +
alpha, owner-egress-blocked) — no harness proposal.

### Earlier

**2026-07-01 (22nd datapoint — factory run, 4th of the day) — `improving`, the biggest run of the day: 4 file-disjoint value-bar-clearing PRs across A1 + risk + design + security, all through the two-gate review.**
An 8-Haiku skeptical scout sweep (A1-residue · correctness/test-isolation · security-default-closed · frontend-honesty · risk/exec · data/backtest · ruff-F7 · self-validation-honesty) surfaced the maximal disjoint set; shipped **4 code PRs + this bookkeeping**: **(#126)** the LOWEST incomplete ROADMAP item **A1** — deleted the dead `/learn` teaching surface (`explainer/tutor/memo`, which built an OpenAI client from a **non-existent** `settings.openai_api_key`) + the dead `paper_simulator.py`, deliberately **KEEPING** `analyst.py` (the declared `llm_analysis` Gemini capability + B4 scaffolding) so the self-validation gate/credential inventory stay untouched; **(#127)** **D2** — revived the structurally-**DEAD** per-strategy drawdown auto-disable (its trade counter only incremented in `record_execution` from a venue payload that never carries a `strategy` tag, so `trades>=min_trades` could never fire → moved counting to `record_pnl`); **(#128)** **design_taste** — `—` not a fabricated `+$0.00` on the header P&L + strategy strip before data loads (the positions-tab real-zero correctly left alone); **(#129)** **security A→A+** — control auth is now **DEFAULT-CLOSED** (no token ⇒ 401) with a `BACKEND_AUTH_DISABLED=1` dev opt-out that config refuses to honour with live money. **The two-gate discipline earned its keep:** #127's **both** Sonnet reviewers caught a stale curated-gate test that would have shipped a RED gate (fixed 1 cycle); #129's Reviewer B caught stale `auth_core` docs + a missing live-opt-out boot-refusal test (fixed 1 cycle); the Opus safety auditors returned **SOUND** (#127, loss caps untouched, no false-disable) and **SAFE** (#129, no fail-open, both live-boot refusals proven). **Anti-padding held HARD:** DROPPED the NaN-settlement guard (impossible state — the parser already coerces non-finite prices to a finite sentinel) and the weather edge-formula fix (a real bug but on a GATED-OFF unvalidated strategy — re-derive it in the B7 re-validation, not standalone), and **ABANDONED** the correctness SQLModel A→A+ fix after PROVING the scout's `extend_existing` fix INSUFFICIENT (the real cause is the dual `app.*`/`backend.app.*` import path — a ~21-file test refactor, deferred as a focused next-run PR, and it's outside the CI-blocking gate). No DoD/floor box ticked; `engine_pct` stays **74** (dead-code + correctness/security/honesty hardening, not new completeness). **Binding constraint STILL OWNER-BLOCKED:** a real less-pinned point-in-time OOS corpus + a real alpha — ALL egress (Polymarket / Kalshi / HuggingFace) `000` in-env again. Owner action **OA-16** (preferred) / **OA-13** / **OA-11** / **OA-15**. NOT `churning`/`stuck` (0 reverts, 1 abandon with a classified `dead_end` reason, 4 durable PRs, 2 clean fix cycles), so no harness proposal — the highest-value next action is the owner's, surfaced via notification. **Next-run notes:** (1) the correctness A→A+ top_gap REAL fix — standardize the test import path (`app.*` vs `backend.app.*`) so the `PredictionPortfolio`-family models register once (`extend_existing` alone proven insufficient — relationship class-registry dup); (2) the ruff correctness-gate **F7** is now clean-able (paper_simulator's F811 gone via #126; only 3 orchestrator/strategies redundant-import F811s remain + add ruff to `requirements-ci.txt` + the `--select E9,F821,F811` preflight step); (3) the last A1 residue: `config.data_provider` Literal + its `/status` echo (config-only); (4) the WeatherArbitrageStrategy edge formula `(1-P)*C - P` should be `C - P` — fix inside B7 re-validation; (5) confirm the Gamma `?id=` + Kalshi contract on the first real fetch (egress-blocked).

### Earlier

**2026-07-01 (21st datapoint — factory run, 3rd of the day) — `improving`, an INTEGRITY + A1 dead-code run: 2 genuine file-disjoint PRs, the headline from a MERGED independent research finding.**
The highest-EV start was NOT a scout sweep — it was the **independent Research Run 11 (#115) integrity finding** (a maker≠checker flag already merged), which named a real BUILDS≠WORKS/honesty gap: `WhaleCopyTradingStrategy` + `WeatherArbitrageStrategy` were wired **unconditionally** into BOTH default scanners despite being untracked in ROADMAP/RESEARCH_MEMORY, with zero tests and zero B3 evidence, and the whale feed ran on a **fabricated `KNOWN_WHALES` seed** (unverifiable addresses; one a self-evident sequential-hex placeholder). Verified against real code, then shipped **2 code PRs + this bookkeeping**: **(#116)** removed the fabricated seed (now `[]` — the feed uses only real `/leaderboard`+`/holders` discovery, real signal or none) and gated both strategies **off-by-default** behind `ENABLE_UNVALIDATED_STRATEGIES` (the `LIVE_TRADING_ENABLED` pattern), with a pure regression suite + ROADMAP **B7** tracking; **(#117)** an **A1** stock-era dead-code removal — deleted the legacy `db/models.py` equity/paper-trading SQLModel stack (registered only by `create_all`, read by nothing) + the yfinance `strategy_tester.py` (imported nowhere), which **also removes the `stock_prices` dual-registration `SAWarning`/fragility** — the exact residue A1's own text named. **The 6-reviewer gate earned its keep twice:** the Opus BUILDS≠WORKS auditor **RAN `init_db`** and confirmed the 10 prediction/durable tables still build with the dual-registration warning gone (#117 SOUND); Reviewer B caught that deleting `strategy_tester.py` **orphaned the `yfinance` dependency** line (fixed in 1 cycle, removed from both requirements files). **Anti-padding held hard:** the frontend "`|| 0` fabrication" scout finding was **dropped after proving the backend always serializes those fields** and the blocks are load-guarded — so every `|| 0` renders a REAL value, and "fixing" a real `0`/`$0.00` to `—` would HIDE real info (wrong direction, unlike the #110 unloaded-summary case); data/risk/security/backtest scouts returned NOTHING-GENUINE on a mature engine. No DoD/floor box ticked; `engine_pct` stays **74** (integrity + dead-code, not new completeness). **Binding constraint STILL OWNER-BLOCKED:** real DoD/floor movement needs a real, less-pinned, point-in-time OOS corpus + a real alpha — ALL egress (Polymarket / Kalshi / HuggingFace) is `000`/blocked in-env again. Owner action **OA-16** (HuggingFace Polymarket-v1, preferred) / **OA-13** / **OA-11** / **OA-15**. NOT `churning`/`stuck` (0 reverts/abandons, 2 durable PRs, 1 clean fix cycle), so no harness proposal — the highest-value next action is the owner's, surfaced via notification. **Next-run notes:** (1) the REST of the A1 stock-era residue is a coherent next chunk (deferred — collides with `routes.py` which #116 touched): the `/learn/*` `QuantExplainer`/`QuantTutor` + `llm/analyst.py` + `llm/memo.py` dead-LLM surface + the `config.data_provider` Literal + the dead `paper_simulator.py` — do it as ONE focused A1 PR; (2) `whale_feed.py:97/115` still logs "using hardcoded seeds"/"N hardcoded" (now always 0) — a stale-log micro-tidy; (3) SELL-path `record_pnl` still a held-to-resolution no-op; (4) a gated-live OPEN-status resting order books a zero-size phantom position (unreachable, live default off — defense-in-depth candidate); (5) confirm the Gamma `?id=` + Kalshi status/price contract on the first real fetch (egress-blocked).

### Earlier

**2026-07-01 (20th datapoint — factory run, 2nd of the day) — `improving`, honest small run: 2 genuine hardening PRs shipped, ~8 candidates rejected after proving each against real code; the binding constraint stays OWNER-BLOCKED.**
Started, per the strongest recent loop-memory lesson ("a deferred next-run note IS the next run's headline — mine the prior auditor's flags first"), by investigating the prior run's 3 NEXT-RUN NOTES by OBSERVING the real code before scouting — and **disproved all 3** as non-value-bar-clearing: `paper_simulator.py` is dead (imported nowhere in `backend/app`, tests, or scripts); `_persist_resolution`'s `get_session()` genuinely commits on clean exit and re-raises-then-logs on failure, so the swallow is reporting-only, **not** a #108-class silent no-op; the Gamma `?id=` live-confirm is egress-blocked. So the prior flags produced no headline — the diligence itself was the value (avoided fixing dead code / a non-bug). Then a **7-Haiku skeptical scout sweep** (data/venues · model/alpha · risk/exec · security/§12 · quality/frontend/artifact · correctness/dead-code · learning-wiring) surfaced ~10 candidates; each was **verified against the actual code** and only 2 were genuine + file-disjoint. Shipped **2 code PRs + this bookkeeping**: (#112) **Kalshi bid/ask bounded to (0,100]** — the `yes_bid`/`yes_ask` checks were a bare `> 0` while the `last_price` branch already bounds to `(0,100]`; an out-of-range cent quote (`yes_bid=150`) passed and **clamped to a fabricated `1.0` certain-outcome price** — now a rejected side falls through to untradeable, never a fabricated certainty (data-honesty, same class as #101 and the Kalshi one-sided-book fix; 2 regression tests **proven to fail on pre-fix code**); (#113) **Equity Curve** — the portfolio chart's hand-rolled bars scaled height as `(value − seriesMin)/range` with **no axis labels**, so a 0.5% move on a ~$1000 book rendered as a ~50% bar; replaced with a recharts **labeled line chart + a starting-balance reference line** (the `design_taste` top_gap the QUALITY_SCORECARD names; the sibling `WeeklyMetricsCard` was already fixed, this one overlooked). **The adversarial gate earned its keep:** #113 Reviewer B (value/honesty) **REQUEST_CHANGES** — the PR claimed parity with the `$0`-anchored P&L sibling but used a pure `dataMin/dataMax` zoom; fixed in **1 cycle** by adding the starting-balance reference line (the honest anchor for a *value* series, vs `$0` for a *P&L delta* series) + disclosing the deliberate deviation, and a fresh re-review APPROVED. **Anti-scarcity AND anti-padding both held:** rejected ~8 candidates after proving each against real code — 4 WalletBehaviorDivergence div-by-zeros (impossible-state: the deployed alphas are non-zero constants with no zero-injection path + the whale-trade data is unfed = padding); the `/learn/explain` "unauthenticated LLM spend" (UNREACHABLE — `has_llm_key` gates on `gemini_api_key` but the explainer builds `OpenAI(api_key=settings.openai_api_key)` and `openai_api_key` **does not exist** on `Settings` → `AttributeError` → template fallback; dead/broken stock-era legacy, not frontend-wired); the price/pnl-history try-except (WRONG DIRECTION — degrading a DB-down 500 to an empty list would **fabricate a "no data" success**, dishonest; a 500 is honest for a read endpoint); and the multi-run-deferred SELL-path `record_pnl` (no new reachability evidence). No DoD/floor box ticked; engine_pct stays **74** (correctness + honesty hardening, not new completeness). **Binding constraint STILL OWNER-BLOCKED:** real DoD/floor movement needs a real, less-pinned, point-in-time OOS corpus + a real alpha — and ALL egress (Polymarket / Kalshi / HuggingFace) is `000`/blocked in-env again this run, so the loop cannot fetch it. Owner action **OA-16** (HuggingFace Polymarket-v1, preferred) / **OA-13** (egress allowlist) / **OA-11** / **OA-15**. NOT `churning`/`stuck` (0 reverts/abandons, durable correct changes), so no harness proposal — the highest-value next action is the owner's, surfaced via notification. **Next-run notes:** (1) SELL-path realized PnL still doesn't feed `risk_manager.record_pnl` — re-verify whether a SELL opp actually realizes PnL in prod (the `executor.execute` path) before building; (2) the `/learn/*` QuantExplainer surface is dead/broken stock-era legacy — a candidate for deliberate A1 stock-residue DELETION, scoped on its own; (3) `paper_simulator.py` remains dead — deletion candidate; (4) confirm the Gamma `?id=` + Kalshi status/price contract on the first real fetch (egress-blocked).

### Earlier

**2026-07-01 (19th datapoint — factory run, 1st of the day) — `improving`, found + fixed a HIGH-severity BUILDS≠WORKS that 5 prior skeptical scout sweeps missed: the settlement path was DEAD in prod.**
The headline was NOT a scout find — it was the prior run's **NEXT-RUN NOTE** (the #104 Opus auditor's flag), investigated FIRST this run by observing the real code. `MarkToMarketEngine.check_resolutions()` looked positions up via `get_market_by_slug(pos.market_id)`, but positions store `market_id=opp.market.id` — the Gamma **numeric** id, a **DISTINCT field** from the URL `slug`. Querying Gamma's `slug` filter with a numeric id matches **nothing**, so `market` was always `None` → **no position ever settled in production**: resolution PnL never realized, the executor loss caps + kill switch never fed on the dominant binary-market loss path (D3/D4), MTM never reconciled. Every unit test passed because the fakes are `def get_market_by_slug(self, _slug)` — **they ignore the argument** (a mock that accepts any input can't catch a wrong-key bug). Shipped **3 file-disjoint code PRs + this bookkeeping**: (#108) **resolution lookup by id** — new `get_market_by_id()` (queries the `id` filter) + a regression test **proven to FAIL on pre-fix code** (position never settles), plus a same-file order-book honesty fix (`get_order_book` no longer fabricates best_bid=0/best_ask=1 for an empty/one-sided book → a bogus 100%-wide spread a strategy reads via `book.spread`); (#109) **§12 input bounds** on the last two unbounded state-mutating request models (`PlaceOrderRequest`/`/execute`, `SubscribeRequest`/`/feeds/subscribe` — size=inf/price=NaN/oversized-list gaps closed); (#110) **frontend honesty** — Portfolio cards show `—` not a fabricated `$0.00`/`0` when the summary hasn't loaded. **The adversarial gate earned its keep: 7 reviewers (2 Sonnet each + 1 Opus safety auditor on the money-path #108), 0 fix cycles** — BOTH #108 Sonnet reviewers AND the Opus auditor independently **reverted the one-line fix and confirmed the regression test fails on pre-fix code** (`0.0 == -50.0`), and the Opus auditor independently validated that deferring the known `_persist_resolution`-swallow issue is SAFE (`executor.positions` is never rehydrated — `load_positions_into_executor` has **0 callers** — and the safety-critical loss counters + kill-switch state persist independently BEFORE `_persist_resolution`, so a swallowed write is reporting-only degradation, not a double-count). **`improving`** (3 PRs vs the prior run's 1, 0 abandoned/revert, a real high-severity defect closed). No DoD/floor box ticked; engine_pct stays 74 (correctness/security/honesty hardening, not new completeness). **Binding constraint STILL OWNER-BLOCKED:** real DoD/floor movement needs a real, less-pinned, point-in-time OOS corpus + a real alpha — and ALL egress (Polymarket / Kalshi / HuggingFace) is `000`/blocked in-env again, so the loop cannot fetch it. Owner action **OA-16** (Polymarket-v1 HuggingFace corpus, preferred) / **OA-13** (egress allowlist) / **OA-11**. NOT `churning`/`stuck` (0 reverts/abandons, durable correct changes), so no harness proposal — the highest-value next action is the owner's, surfaced via notification. **Next-run notes:** (1) `paper_simulator._resolve_market` (`paper_simulator.py:366`) has the SAME slug-vs-id call but degrades safely (falls back to a `get_markets()` scan matching `m.id`/`m.condition_id`) — a follow-up tidy, give it `get_market_by_id`; (2) `_persist_resolution` still swallows DB failures (reporting-only divergence, non-safety-critical this run); (3) confirm the Gamma `?id=` filter param on the first real fetch (documented-but-unverified-live, egress-blocked).

### Earlier

**2026-06-30 (18th datapoint — factory run, 5th of the day) — `steady`, ONE genuine side-effect-integrity fix; the binding constraint is now OWNER-BLOCKED and convergence needs an owner data action.**
A deliberately SKEPTICAL 4-Haiku scout sweep (data/alpha · risk/safety · backtest/learning · quality/frontend lenses) — primed with "default to nothing-genuine; 4 hardening runs already today; egress blocked" — surfaced exactly **ONE** genuine, file-disjoint, value-bar-clearing fix; **3 of 4 scouts returned NOTHING-GENUINE**. Shipped **1 code PR + this bookkeeping**: (#104) **side-effect integrity on the settlement path** — `MarkToMarketEngine.check_resolutions()` settled a held position at a fabricated `winning_price=0.0` (a phantom TOTAL loss) whenever the held `token_id` was ABSENT from a resolved market's `outcomes` (a data inconsistency: a re-resolved/stale market, a malformed/short `clobTokenIds` array). That invented loss fed `record_realized_pnl()` and could **AUTO-TRIP the kill switch on a fiction** (halting the whole bot), and cached the position resolved so it never reconciled — the same honesty class as #101 ("a missing quote is not a 50/50 market") and #95 (no phantom fill). Fixed with a `None` sentinel: settle only when the token is present (a found loser still settles at real 0.0), else log loudly + skip + leave uncached to retry. **The adversarial gate earned its keep with NO fix cycle:** 2 Sonnet reviewers + 1 fresh Opus safety auditor all cleared it first pass — and **two of them INDEPENDENTLY REPRODUCED** the kill-switch-on-fiction failure against pre-fix code (`[KILL SWITCH] ACTIVATED — realized $-50.00 breaches -$5.00`), proving it's a real defect, not padding. **`steady`, honestly:** 1 PR vs the prior run's 4 — after 5 runs today the engine-hardening barrel is near-empty, so honest output is small (anti-padding > volume). The one owner-gated find (NearCertaintyStrategy 72h-vs-720h, ROADMAP F1) was SURFACED, not built — it's a trading-behavior decision the loop must not make unilaterally. **THE headline: the binding constraint is OWNER-BLOCKED.** No DoD/floor box can move without a real, point-in-time, less-pinned OOS corpus + a real alpha — and ALL egress (Polymarket / Kalshi / HuggingFace) is blocked in-env again this run, so the loop cannot fetch it. Real convergence now needs the owner to action **OA-16** (download the Polymarket-v1 HuggingFace corpus — 1.3M markets, CC-BY-4.0, the preferred bypass) or **OA-13** (widen egress) / **OA-11**. This is NOT `churning`/`stuck` (0 reverts, 0 abandoned, a durable correct change), so no harness proposal is warranted — but the highest-value next action is now the owner's, surfaced via notification. **Next-run note:** the Opus auditor flagged two pre-existing, orthogonal latent issues to investigate — (1) `check_resolutions` fetches via `get_market_by_slug(pos.market_id)` but positions store the Gamma numeric `id`, not the slug, so if `id != slug` resolution may never fire in prod; (2) `_persist_resolution` swallows failures, so in-memory vs DB settlement can diverge.

### Earlier

**2026-06-30 (17th datapoint — factory run, 4th of the day) — `improving`, a data-integrity + security hardening sweep; the adversarial gate caught a stale-quote bug and named the one consumer missing the active-gate.**
An 8-Haiku scout sweep across tracks A–G (data-integrity / security / correctness / artifact-freshness lenses) surfaced the maximal file-disjoint, value-bar-clearing set; shipped **4 code PRs + this bookkeeping**, all ship-critical-dimension (functional-reality / security / artifact-integrity) hardening: (#99) **§12** — bound the remaining UNBOUNDED read endpoints + quant-model float params (resource-exhaustion + NaN/inf-into-model) and sanitize the unauthenticated `/status` raw-exception leak; (#100) **BUILDS≠WORKS** — the live WS price path did a bare `float()` with NO validation, so NaN/inf/out-of-range could silently poison the in-memory price cache that strategies+executor read (the batch path has DQV; the feed did not) → coerce + range-check every field, skip the update on a bad primary price; (#101) **side-effect integrity** — the Polymarket parser FABRICATED a `0.5`/empty-token "tradeable 50/50" market that PASSES DQV (the data analog of a fake fill) → now marked untradeable (`active=False`), un-fabricated data fails DQV; (#102) **honesty-invariant completion** — gate `CalibrationBucketStrategy.scan` on `market.active` (the one consumer the parse fix relied on that wasn't gating). **The gate earned its keep twice:** (1) Reviewer A returned REQUEST_CHANGES on #100 — the `last_trade_price` timestamp refreshed even when ALL fields were rejected, so a garbage flood could keep a stale quote looking fresh (defeating the 300s staleness eviction); fixed in ONE cycle + a regression test. (2) the Opus data-integrity auditor (**SOUND** — could not get a fabricated/invalid price onto any deployed tradeable path) named `CalibrationBucketStrategy` as the lone consumer missing the active-gate → shipped as #102. **Honest maximal selection under a mature engine (engine_pct 73→74) + an egress-blocked binding constraint:** rejected ~20 scout candidates as padding / false-positive (a kelly seed_hash "bug" that's actually the documented strategy-fn exclusion) / dead-unwired (paper_simulator) / already-fixed (scan-guard #96) / wrong-direction (loosening a loss cap) / already-deferred (SELL-path held-to-resolution no-op) / sensitive-staged (F7 lint ratchet). **All egress (HuggingFace + Polymarket + Kalshi) is 403 in-env this run** — confirming OA-11/OA-15/OA-16 are owner-scope, not a loop wall. No DoD/floor box ticked — hardening, not a validated edge; binding constraint unchanged, so no harness proposal warranted.

### Earlier

**2026-06-30 (16th datapoint — research run, 3rd of the day) — `improving`, Polymarket-v1 HuggingFace dataset identified as a bypass for the primary data blocker; EXP-003 proposed on domain-calibrated political miscalibration.**
Academic synthesis across five 2026 papers identified three actionable findings: (1) **Polymarket-v1** (arxiv 2606.04217) — 1.3M resolved markets on HuggingFace under CC-BY-4.0 with market metadata + outcomes in the `daily_aligned/` Parquet layer; no Polymarket API egress required; proposed as **OA-16** (the preferred path to bypass OA-11). (2) **Le 2026** (292M trades) confirms domain-specific calibration: political markets are PERSISTENTLY underconfident at all horizons (bilateral partisan cancellation compresses prices toward 50%); proposed **EXP-003** (domain-calibrated political strategy, same mechanism as the already-built CalibrationBucketStrategy). (3) **Prediction Arena + PolyBench**: autonomous LLM trading on Kalshi loses money (-16% to -30.8%); Gemini-3-Flash achieves +6.2% CWR on Polymarket — this REFINES B4 (targeted Gemini research tool for specific domains, not autonomous trader). Correctly classified insider-signal copying as out-of-scope; proposed an in-scope defensive adverse selection filter. No code PRs this run (research-only). No harness proposal warranted — the data blocker now has two owner paths (OA-11 OR new OA-16), so the loop has forward motion without a rule change.

### Earlier

**2026-06-30 (15th datapoint — factory run, 2nd of the day) — `improving`, the adversarial gate killed a fail-safe that was DEAD CODE in prod, and a stale-base hazard was caught before it shipped.**
An 8-Haiku scout sweep across tracks A–G surfaced the maximal file-disjoint, value-bar-clearing set; shipped **3
code PRs + this bookkeeping**, all ship-critical-dimension hardening: (#96) **API security** — guard the lone
unguarded state-mutating route `/prediction-markets/scan`, bound user inputs, and bounds-validate `risk/config` so a
non-positive daily-loss cap (which would DISABLE loss protection) is rejected 422; (#95) **side-effect integrity** —
stop fabricating a phantom fill (empty token_id) for multi-leg `outcome_idx == -1` arbitrage baskets, skipping them
honestly until per-leg execution (B1) exists; (#94) **executor fail-closed** — a durable-store rehydrate failure now
trips the kill switch instead of silently resuming a halted bot. **The gate earned its keep twice:** (1) a fresh Opus
live-safety auditor returned **NOT-SAFE** on #94 — the new fail-closed branch only fired if `load()` raised, but the
production `ExecutorStateStore.load()` swallowed every error and returned `None`, so it was **dead code against the
only store that ships** (a DB-down restart would still resume un-halted); fixed by making `load()` distinguish ABSENT
(None) from UNREADABLE (raise) + a REAL-store fail-closed test + a defense-in-depth empty-token reject, and a fresh
**re-audit returned FIX-HOLDS**. (2) A **stale local default-branch ref** (lagging origin by ~25 commits, pre-auth)
nearly based two PRs on old code — caught when a security scout's "/scan is the only unguarded route" contradicted the
session-start read (which showed every route guarded); recovered by fetching origin, resetting the ref, and rebasing,
which in turn surfaced a false-green test the stale base had hidden. **No DoD/floor box ticked** — hardening across
security/§12, run-risk-readiness (D3/D4), and side-effect integrity (B1); engine_pct 72→73. Binding constraint
unchanged: the 7-day-lead OOS corpus + a real alpha (OA-11/OA-15, owner/egress-scope), so no harness proposal
warranted. unmet=[].

### Earlier

**2026-06-30 (14th datapoint — factory run) — `improving`, the adversarial gate caught TWO real defects the tests passed over.**
An 8-Haiku scout sweep across tracks A–G surfaced the maximal file-disjoint, value-bar-clearing set; shipped **2
code PRs + this bookkeeping**: (#92) ROADMAP **A3 — a second-venue Kalshi DATA adapter** (`kalshi_client.py` +
`kalshi_history_fetcher.py`) behind the SAME `Market`/`Outcome`/`HistoricalMarket` interface, with the same
structural anti-leakage guarantee as the Polymarket fetcher, fully offline fixture-tested, no new credential; and
(#91) a **deep-audit hardening pass** (live-only control-auth boot-guard; category-cap under-count fix; risk-score
div-by-zero guards; MTM client reuse; API exception-detail leak sanitization). **The gate earned its keep twice:**
(1) a fresh Opus **parsing auditor returned BROKEN** on the Kalshi adapter — the offline fixtures had encoded
Kalshi's *request-side filter* words (`"open"`/`"finalized"`) as if they were *live response* values, so the
adapter would have dropped **every live market** (a BUILDS≠WORKS: 45 green tests proving nothing about real
behavior); fixed to the documented response contract (`active`/`settled`/`determined`, `settled` discovery filter,
one-sided-book honesty, `p=0` tick fix) + an honest "documented-but-unverified-live" disclosure + a loud
warning-on-unknown-status net, and a **re-audit returned FIX-HOLDS**; (2) an Opus **live-safety auditor** found the
category-cap fix used the wrong cap field (`RiskConfig.max_single_position_usd` $50 vs the executor's real
`max_position_usd` ~$5) → a ~10x over-reservation, corrected to read the executor's actual cap. Both fixed in ONE
cycle each (≤2-cycle brake), merged on green required checks. **Anti-scarcity + anti-padding both held:** deferred
B6 strategy-control + D6 reconciler (genuinely buildable but collide with #91 on `orchestrator.py` — the disjoint
rule, not scarcity), F5 Playwright (real exec risk + can't CI-gate; a focused run suits it better), and F7 lint
(low value / risky on the trading path); a deep-audit scout found NO real defect beyond the audit findings, so none
was invented. No DoD/floor box ticked — venue infra + hardening, not a validated edge; binding constraint stays the
7-day-lead OOS corpus (OA-11 Polymarket, new OA-15 Kalshi — owner/egress-scope), so no harness proposal warranted.

### Earlier

**2026-06-29 (13th datapoint — factory run) — `improving`, the adversarial gate caught a real BUILDS≠WORKS the tests missed.**
Shipped 3 file-disjoint PRs from an 8-scout sweep, all driving named QUALITY_SCORECARD top_gaps: (A) control-path
hardening — durable kill-switch + realized-PnL persistence so a restart can't un-trip the halt or reset the loss
budget (run-risk-readiness top_gap), REST venue-fill validation (side-effect integrity, D1), and a degrade-safe
shared-secret on the 12 state-mutating backend routes (security top_gap, OA-14); (B) frontend design-taste — deleted
the dead `Math.random()` `equity-chart.tsx` and fixed the WeeklyMetricsCard P&L axis to always include $0; (C) this
bookkeeping. **The headline win — an Opus live-safety auditor proved a BUILDS≠WORKS the unit tests passed over:** the
durable audit-log/registry/executor tables were never created at startup (`init_db`'s `create_all` ran *before* their
lazy import), so the "durable" persistence silently no-op'd in prod. Root-caused (import the table modules in `init_db`
before `create_all`) and pinned with a cold-start subprocess regression test. **The gate earned its keep again:** 2
Sonnet reviewers + 3 Opus auditors (auth REAL+SOUND, side-effect SOUND, live-safety SAFE) flagged 2 MUST-FIX (auth
fail-open log→CRITICAL; deactivate-persist race) + several hardening nits — all fixed in ONE consolidated cycle (≤2-cycle
brake), then re-verified green. **Anti-scarcity + anti-padding both held:** dropped D2-SELL-path (a no-op in the current
held-to-resolution flow — would be a fake control) and the alpha/egress-blocked items (B1/E7/B3-derivation/C2) on the
value+disjoint rules, not invented. No DoD/floor box ticked — hardening, not a validated edge; binding constraint stays
OA-11 (7-day OOS corpus, owner/egress-scope), so no harness proposal warranted.

### Earlier

**2026-06-29 (11th datapoint) — `improving`, the loop can now mechanically prove it validates the app.**
Added a **self-validation coverage gate** (`docs/ci/SELF_VALIDATION.md` manifest +
`scripts/check_self_validation.py`, blocking preflight step 9d): every *active* capability must be
really `validated`/`gated_off`/`degrades_safely`, and every credential the code reads must be
declared — a **new, undeclared** credential **surfaces + blocks every PR** (proven end-to-end with a
simulated `KALSHI_API_KEY`). By design the gate needs zero keys to validate the active app; keys are
only for *activation* (live = human-core) or *enhancement* (Gemini = optional). The factory routine
now maintains the manifest as part of shipping any capability. 8 regression tests; seeded green.

### Earlier

**2026-06-29 (10th datapoint — factory run) — `improving`, the binding constraint now has a BUILT mechanism, and disjoint discipline held under contention.**
Shipped 3 file-disjoint code PRs + 1 bookkeeping from an 8-scout sweep: the FIRST `model_prob != crowd`
alpha mechanism (`CalibrationBucketStrategy` — per-bucket empirical calibration, leakage-safe, abstaining,
3-Opus-auditor-clean), the E5/E2 learning engines WIRED into the resolved stream + read-only endpoints
(honest insufficient-data/degenerate paths), and a frontend metrics dashboard rendering every `/metrics/*`
endpoint with honest empty/degenerate states. **Disjoint discipline under contention:** 3 genuinely-buildable
items (B6, per-leg arb, A4) all wanted `orchestrator.py`, which the E5/E2 PR already owned — correctly deferred
to a later run (the disjoint rule, NOT scarcity); Kalshi A3 + lint F7 were dropped on the value bar (premature /
cosmetic). **Anti-padding both ways:** a deep-audit scout found NO real defect, so no defect-PR was invented.
**The adversarial gate earned its keep:** both Sonnet reviewers caught a shared-mutable-model-in-closure bug
(fresh-model-per-call fix + a re-fit test) and an Opus auditor named an idealized-test honesty gap (added a
cost-band-suppression test) — all fixed in ONE cycle, shipped on mechanical verification (≤2-cycle brake).
No DoD/floor box ticked — B4a is the mechanism for an edge, not a validated edge; the highest-EV unlock stays
OA-11 (the 7-day OOS corpus, owner/egress-scope), so no harness proposal is warranted.

### Earlier

**2026-06-29 (9th datapoint — research run) — `steady`, EXP-002 proposed with zero code waste.**
Research-only run. Fixture calibration audit (54 records) + academic synthesis (Le 2026 Kalshi
calibration decomposition, PolyBench LLM-ensemble results, 3%-of-traders price-discovery study) converged
on EXP-002: the 54-record corpus is pinned at 48h and cannot test any calibration hypothesis. The
fetch_polymarket_history.py script already accepts `--decision-lead-days` (no code change needed); re-running
OA-11 with 7-day lead is the single highest-EV owner action to unlock all calibration alphas. Correctly
classified a fixture calibration anomaly (near-certainty-NO bucket: stated 0.8%, empirical 5.9%, p≈0.029)
as "insufficient data" — N=34 with ~2 YES outcomes, in-sample, biased sample; not an edge claim.
No code PRs; 1 bookkeeping PR (RESEARCH_MEMORY + GROWTH_STATUS + LOOP_HEALTH).

### Earlier

**2026-06-29 (7th datapoint — factory run) — `improving`, the model/strategy layer got better AND the adversarial
gate earned its keep again.** Shipped 4 file-disjoint PRs (#63–#66) from an 8-scout sweep: hardened the
cross-market keyword relatedness screen (B5), made the calibration gate's multiple-comparison correction
code-enforced (B2 Bonferroni), and built the first concrete learning-loop engines — an alpha lifecycle
registry with a fail-loud integrity gate (B3) and a per-strategy realized-PnL attribution primitive (E6).
**Anti-padding worked:** an 8th scout's alleged cost_model "extreme-price" bug did not survive scrutiny
(the floor is conservative, the safe direction), so it was dropped pre-build rather than shipped as busywork.
**The two-gate discipline caught real defects across two fix cycles:** a fresh Opus auditor broke the first
B5 hardening cut (entity-gating still admitted venue/nationality-shared pairs AND dropped acronym subjects)
→ simplified to a broad-stopword content screen; Sonnet/Opus reviewers caught a naive-timestamp round-trip
that broke determinism on non-UTC hosts and a frozen-dataclass-with-mutable-dict — all fixed within the
≤2-cycle brake, then merged on a green required check (no 3rd audit on a strictly-more-conservative change).
Gate strengthened 176→~290 enforced tests. No DoD/floor box ticked — these are engine/integrity pieces; the
binding constraint stays loop-buildable (a real alpha + wiring the new engines), so no harness proposal is
warranted.

### Earlier

**2026-06-29 (6th datapoint) — `improving`, closed the data-access gap hands-off.** Staged the
real-data refresh automation (OA-13): the fetcher now `--merge`s to ACCUMULATE a growing OOS corpus,
and a scheduled GitHub Action (or the env egress allowlist) keeps it fresh without a human — the
loop's egress wall stops being a recurring manual residual. Honest scope: the permanent toggle is
owner-irreducible (platform/`.github/` settings), so it's staged + tracked, not faked.

### Earlier

**2026-06-29 (5th datapoint) — `improving`, the adversarial gate earned its keep.** Shipped 5
file-disjoint PRs (#56–#60) from an 8-scout sweep: C5 metrics wired end-to-end, a venue-layer
fail-closed live gate, a real-data reproduction canary, a forensic strategy-audit harness, and
enforced LLM timeout + spend cap. **0 abandoned, 0 reverts.** The two-gate discipline caught real
defects pre-merge: 2 Sonnet reviewers flagged precision bugs in the metrics aggregator (iterator
double-consume; float-equality degenerate check) and — most importantly — a fresh Opus honesty
auditor **broke** the strategy-audit claim ("0 signals on the real sample" was FALSE: the harness's
own boilerplate question text produced ~98 phantom signals). All were fixed in ONE consolidated
cycle and are now guarded by loud regression tests; the blocking gate was extended from 99 to 176
enforced tests so the new safety/metrics/canary/audit checks bind every future change. The binding
constraint stays loop-buildable (a real decision-time alpha producing model_prob != crowd), not an
environmental wall — so no harness proposal is warranted.

### Earlier

**2026-06-28 (4th datapoint) — `improving`, the required check now has teeth.** Hardened the
gate enforcement so it actually binds the loop: `enforce_admins=true` (an `--admin` merge can no
longer bypass), `strict=false` (parallel file-disjoint PRs still auto-merge), repo
`allow_auto_merge=true`, and the merge protocol switched to `gh pr merge --squash --auto` in
ROADMAP "Shipping protocol" **and in all three PR-merging routine prompts** (factory / research /
auditor) — so every future autonomous run waits for CI instead of force-merging. Also shipped a
prod boot-guard: `E2E_DISABLE_RATE_LIMIT` can never be active with `LIVE_TRADING_ENABLED`
(`enforced_in_ci: true`). No open proposals.

### Earlier

**2026-06-28 (3rd datapoint) — `improving`, first harness proposal raised AND resolved.** Staged
the required-check + lint-at-zero work (`docs/ci/PROPOSED_CI.md`), raised harness issue **#51**
for the one piece the loop can't self-apply (branch protection = admin scope) — and the owner
authorized it the same day, so it was applied: the branch is now protected requiring
`code + safety gate (blocking)`, so a change that regresses the live gate / kill switch /
paper-pipeline reproduction **can no longer auto-merge** (OA-12 done, `harness_proposals_open`
back to 0). Held lint-at-zero *off* (166 ruff findings in trading code) rather than red-block the
now-required gate — exactly "verify green before requiring" (ratchet tracked as ROADMAP F7). The
META channel did its full job: raise → action → close, not a silent wall and not a hanging issue.

### Earlier

**2026-06-28 (2nd datapoint) — `improving`.** Throughput is healthy (**43 merged / 0 reverts /
0 abandoned** over 7 days), and the wall that loomed last run got knocked down: the **Polymarket
egress block** (403 at the env proxy) that made real-data OOS validation impossible was resolved
*for data access* by running the leakage-safe fetcher from a network-permitted host (54 real
records, pipeline reproduces — see `OA11_REAL_DATA_VALIDATION.md`). It did NOT recur as a
convergence wall, so it is **not** trending toward `stuck`; the residual is only *periodic
refresh scheduling* (OA-11, `blocked_owner`). Crucially the binding constraint **moved** from
data-access to "need a real alpha model" — which is **loop-buildable** (track B), i.e. forward
motion, not a dead-end. Per FACTORY_STANDARD §10b, had that same wall blocked a second run with
no progress, the rule would have flipped the signal to `stuck` and required one
`loop: harness improvement proposal`. It didn't — so none is opened, honestly.
